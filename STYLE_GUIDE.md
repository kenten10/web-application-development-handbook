# スタイルガイド

この文書は、『WEBアプリケーション開発者になれる解説書 ― 基礎から一流までの30章 ―』の本文について、日本語表記、用語、コード、図表、注記の書き方を定める。各ルールにはIDを与え、`pnpm run validate:style` が機械検査する。章レビューと編集校正 (KEN-59) は、このIDを使って指摘する。

## 0. この文書の使い方

### 0.1 正本と生成物

| 対象 | 役割 |
|---|---|
| `STYLE_GUIDE.md` | ルール本文の正本。人が読む形の定義 |
| `config/style-guide.json` | ルールID、判定の対象範囲、語彙一覧、ベースラインの正本 |
| `config/glossary.json` | 用語の正表記・別表記・定義の正本 |
| `GLOSSARY.md` | 用語集。`config/glossary.json` からの生成物。直接編集しない |
| `STYLE_BACKLOG.md` | 未修正の違反一覧。検査結果からの生成物。直接編集しない |
| `scripts/validate-style.mjs` | ルールの機械検査 |
| `scripts/apply-style-fixes.mjs` | 機械適用できるルールの一括適用 |

`STYLE_GUIDE.md` と `config/style-guide.json` のルールIDが食い違うと S-META-001 で失敗する。ルールを追加・削除するときは両方を同時に更新する。

### 0.2 検査の対象範囲

検査対象は `00-front-matter.md` と本文7ファイル (`02-part1-foundations.md` 〜 `08-part7-practice.md`) とする。索引語の照合だけは `10-index.md` を対象にする。

次は対象外とする。

- コードブロックの内側、インラインコード、リンク先URL、リポジトリ内パス。これらは記述内容そのものであり、表記規則を適用しない。
- `01-toc.md`、`10-index.md`、`LEARNING_LEVELS.md`、`LEARNING_PATHS.md`、`CHAPTER_TEMPLATE.md`、`CODE_EXERCISES.md`、`code/chXX/README.md`。いずれも生成物であり、正本側で表記を担保する。
- `09-references.md`。引用形式は KEN-57 が定めた規約に従い、書名・原題の記号をそのまま保つ。
- `README.md`、`CONTRIBUTING.md` などの運用文書。読者ではなく執筆者向けの文書であり、文体規約が異なる。

### 0.3 違反の扱い

ルールは `error` と `baseline` の2種類に分かれる。

- **error**: 1件でも検出したら失敗する。本書は現時点でこの分類の違反を0件にしてある。
- **baseline**: 既存の違反件数を `config/style-guide.json` の `baselines` にファイル単位で記録し、その件数を超えたら失敗する。既存分は警告として報告し、`STYLE_BACKLOG.md` に全件を残す。新規混入だけを止める運用とする。

`baseline` の違反を修正したら、`pnpm run validate:style --update-baseline` と `--backlog` を実行して上限と一覧を下げる。上限を引き上げてはならない。

KEN-59 の全文校正で S-TERM-002 / S-EN-001 / S-VAGUE-001 の既存 338 件をすべて解消したため、現在の上限は全ファイルで 0 である。以後は新規の違反がそのまま警告として現れる。

## 1. 日本語表記 (S-JA)

### S-JA-001 句読点は「、」「。」に統一する

読点は `、`、句点は `。` を使う。全角カンマ `，` と全角ピリオド `．` は使わない。箇条書きの各項目は、文が1つで終わるなら句点を省いてよいが、同じ箇条書きの中で有無を混在させない。

### S-JA-002 英数字は半角で書く

英字と数字は半角で書く。全角英数字 (`Ａ`〜`Ｚ`、`ａ`〜`ｚ`、`０`〜`９`) は使わない。単位、バージョン番号、ステータスコードも半角にそろえる。

### S-JA-003 感嘆符・疑問符は半角で書く

`!` と `?` は半角で書く。全角の `！` `？` は使わない。技術文書では感嘆符を原則使わない。疑問符は、読者へ問いを立てる箇所と、疑問文をそのまま示す箇所に限る。

### S-JA-004 括弧は半角 () に統一する

補足を囲む括弧は半角 `(` `)` を使う。全角括弧 `（` `）` は使わない。会話・引用・強調のかぎ括弧は `「」`、書名は `『』` を使い、これらは全角のままとする。

### S-JA-005 全角スペースを使わない

字下げ、位置合わせ、強調のいずれの目的でも全角スペース (U+3000) を使わない。位置合わせが必要な図はコードブロックへ入れる (S-CODE-004)。

### S-JA-006 半角括弧の外側に半角スペースを1つ置く

和文と半角括弧が隣接するときは、括弧の外側に半角スペースを1つ置く。

- 正: `次のレベル (シニア・テックリード) に進む`
- 誤: `次のレベル(シニア・テックリード)に進む`

括弧の隣が英数字・記号の場合は空白を強制しない。`useState()` のような関数呼び出しの記法を壊さないためである。

### S-JA-007 本文の文体は常体に統一する

本文は常体 (だ・である調) で書く。「です」「ます」「ません」「ましょう」「でしょう」「ください」「でした」「ました」で終わる文を地の文に置かない。

次は例外とする。

- `「メールアドレスを入力してください」` のように、かぎ括弧で囲んだUI文言・エラーメッセージ・発話の例示。
- `README.md`、`CONTRIBUTING.md`、`code/chXX/README.md` などの運用文書。これらは敬体で書く。

## 2. 記号 (S-SYM)

### S-SYM-001 全角ダッシュは ― (U+2015) を使う

副題の区切り、言い換え、挿入句には `―` (U+2015 HORIZONTAL BAR) を使う。`—` (U+2014 EM DASH) と `–` (U+2013 EN DASH) は使わない。節タイトルの副題は `14.18 VACUUM の詳細 ― PostgreSQL 運用の死活問題` の形にそろえる。

### S-SYM-002 波ダッシュは 〜 (U+301C) を使う

範囲を示すときは `〜` (U+301C WAVE DASH) を使う。`～` (U+FF5E FULLWIDTH TILDE) は使わない。数値の範囲でハイフンを使うか波ダッシュを使うかは、同一の表・同一の節の中でそろえる。

## 3. コード・図表・注記 (S-CODE)

### S-CODE-001 コードブロックには必ず言語を指定する

すべてのフェンス付きコードブロックに言語識別子を書く。構文強調を意図しないブロック — テキスト図、コンソール出力、ディレクトリ構成、HTTPメッセージの例 — は `text` を指定する。

````markdown
```typescript
const server = createServer(handler);
```

```text
[HTML] → [DOM] → [Render Tree] → [Layout] → [Paint]
```
````

### S-CODE-002 コードブロックの言語識別子は許可一覧から選ぶ

使ってよい言語識別子は `config/style-guide.json` の `fenceLanguages` に列挙する。新しい言語を使うときは、まず一覧へ追加する。同じ言語に複数の綴りがある場合 (`js` と `javascript` など) は、章の中で統一する。

### S-CODE-003 表はGFMのパイプ表とし区切り行を置く

表はGitHub Flavored Markdownのパイプ表で書き、ヘッダ行の直後に区切り行 (`|---|---|`) を置く。セル内で改行しない。セル内にパイプを書くときは `\|` へ退避する。数値の列は区切り行で `---:` を指定して右寄せにしてよい。

### S-CODE-004 図はテキスト図としてコードブロックに置く

図は `text` を指定したコードブロックの中にテキストで描く。画像の埋め込み記法 (感嘆符 + 角括弧 + 丸括弧) は本文で使わない。配布形式が複数あり、画像の再現性と代替テキストの管理コストが高いためである。罫線は罫線素片 (`┌ ─ │ └ ├ →`) を使い、位置合わせに全角スペースを使わない。

### S-CODE-005 注意・補足はブロック引用の見出し付きで書く

注意・補足・警告のラベルは `**注意**:` `**警告**:` `**補足**:` `**重要**:` の4種類に限る。ラベルは強調の内側に置き、本文は強調の外へ出す。

- 正: `**注意**: K8sのSecretはBase64エンコードしてあるだけで、暗号化ではない。`
- 誤: `**注意点:**` / `**注意: K8sのSecretは暗号化ではない。**`

演習の「本番利用時の警告」と章の「学習ガイド」は `config/exercises.json` と `config/chapter-guides.json` から生成するブロック引用が担う。この2つを手で書かない。

## 4. 用語 (S-TERM)

### S-TERM-001 用語集の非正規表記を使わない

`config/glossary.json` で `severity` が `error` の別表記を本文に書かない。正表記は次の順で決める。

1. 製品名・プロジェクト名・規格名は、提供元の公式表記に従う。
2. 外来語の日本語表記は、語ごとに1つへ固定する。長音符の有無は語ごとに判断し、書物全体で機械的に統一しない。
3. 判断が割れる語は、`config/glossary.json` の `definition` と作業レポートに理由を残す。

`exceptions` に登録した文字列 (製品名の一部、コマンド名、識別子) は検査から除く。

### S-TERM-002 用語集の非推奨表記を新規に増やさない

`severity` が `warn` の別表記は、同義の訳語が併存している語である。新しく書く文章では正表記を使う。KEN-59 で既存箇所を `pnpm run apply:style-fixes` により正表記へそろえた (223件)。`CSS-in-JS` や `直列化異常` のように、別表記が固有名詞・専門用語の一部になる語は `config/glossary.json` の `exceptions` で除外する。

## 5. 英語併記と略語 (S-EN)

### S-EN-001 略語は初出で英語正式名称を併記する

`config/glossary.json` で `expandOnFirstUse` が真の略語は、本文の地の文で最初に現れる段落で `略語 (英語正式名称)` の形を示す。

- 正: `URI (Uniform Resource Identifier) はリソースを一意に識別する。`
- 誤: `URIはリソースを一意に識別する。` (それ以前に正式名称を示していない場合)

「初出」は本書全体で1回とし、章ごとに繰り返さない。見出し、表、箇条書きのチェック項目、生成ブロックは初出の判定に数えない。説明ではなく参照だからである。

英語を併記するときは、日本語のあとに半角括弧で置く (`アクセシビリティ (a11y)`)。英語を先に置き日本語を括弧に入れる形は、原語が固有名詞のときに限る (`Closure (クロージャ)`)。

## 6. 曖昧表現 (S-VAGUE)

### S-VAGUE-001 曖昧表現は時点・出典・判定基準を添えて使う

「AI時代」「モダン」「主流」「最新」「最近」「近年」「一般的に」「事実上の標準」「デファクト」「業界標準」「ベストプラクティス」「人気」「時代遅れ」「革命」「最強」「圧倒的」「劇的」「爆発的」「驚くほど」「急速に」といった語は、禁止しない。ただし、次のいずれかを同じ段落に添えたときだけ使う。

1. **時点**: 西暦年、版番号、「2026年時点」のような時点の明示。
2. **出典**: `[Fielding, 2000]` `[RFC 9110]` のような参考文献キー、または一次資料名。
3. **判定基準**: 数値、条件、観測方法など、読者が自分で確かめられる基準。

- 正: `2026年時点では PostgreSQL を主、Redis をキャッシュとする構成が広く採られる。`
- 正: `Fielding の定義 [Fielding, 2000] では、RESTは6つの制約の集合である。`
- 誤: `モダンなWebアプリでは、これが主流である。`

添えるものがない場合は、条件と目的を含む説明へ置き換える。「速い」「重い」のような評価語も、比較対象と測定条件を書けないなら使わない。

対象語の一覧は `config/style-guide.json` の `vagueExpressions`、根拠と認める記述の判定は同ファイルの `evidencePatterns` にある。

## 7. 索引 (S-IDX)

### S-IDX-001 索引語に非正規表記を含めない

`10-index.md` の索引語は、用語集の正表記と一致させる。索引語は本文側の `<!-- handbook:index {"group":"...","term":"..."} -->` メタデータから生成するため、修正は本文側で行い、`pnpm run generate:handbook` で再生成する。

括弧付きの限定 (`ヘッダ (HTTP)`、`JSONB (PostgreSQL)`) は、括弧の中身も正表記にそろえる。

### S-IDX-002 索引必須の用語は索引に存在させる

`config/glossary.json` で `indexTerm` が真の見出し語は、索引に同じ表記で存在しなければならない。索引語を消す変更が用語集の見出し語を孤立させないための回帰ガードである。

## 8. 運用 (S-META)

### S-META-001 ルールIDはSTYLE_GUIDE.mdと一致させる

`config/style-guide.json` の `rules` に登録したIDは、この文書に必ず記載する。この文書に現れるIDは、必ず `rules` に登録する。片方だけの追加は失敗する。

## 9. 章レビュー用チェックリスト

章を通読するときは、次の順に確認する。左端の括弧に対応するルールIDを示す。機械検査で見つかるものは先に `pnpm run validate:style` を実行し、残りを人手で確認する。

### 9.1 機械検査で確認する

- [ ] `pnpm run validate:style` がエラー0で終わる (全ルール)
- [ ] `pnpm run generate:glossary:check` が成功する (S-TERM-001)
- [ ] `pnpm run check:handbook` がエラー0で終わる
- [ ] `STYLE_BACKLOG.md` の件数が 0 のままである (S-TERM-002、S-EN-001、S-VAGUE-001)
- [ ] 曖昧語を部分文字列として含む専門用語 (「最近傍」など) は `config/style-guide.json` の `vagueExceptions` に登録してある

### 9.2 表記と文体を人手で確認する

- [ ] 地の文が常体で書かれている。UI文言の例示だけがかぎ括弧の中で敬体になっている (S-JA-007)
- [ ] 補足の括弧が半角で、和文との間に半角スペースがある (S-JA-004、S-JA-006)
- [ ] 副題と挿入句のダッシュが `―` にそろっている (S-SYM-001)
- [ ] 箇条書きの句点の有無が同じ箇条書きの中でそろっている (S-JA-001)

### 9.3 用語を人手で確認する

- [ ] 章で新しく導入した用語が `config/glossary.json` にある (S-TERM-001)
- [ ] 同義語を混ぜていない。訳語を選び直した箇所がない (S-TERM-002)
- [ ] 略語の初出で英語正式名称を示している。2回目以降で繰り返していない (S-EN-001)
- [ ] 製品名・規格名が公式表記になっている (S-TERM-001)
- [ ] 章で導入した主要概念に索引メタデータが付いている (S-IDX-001、S-IDX-002)

### 9.4 コード・図表を人手で確認する

- [ ] すべてのコードブロックに言語指定があり、テキスト図が `text` になっている (S-CODE-001、S-CODE-004)
- [ ] 言語指定が章の中で統一されている (S-CODE-002)
- [ ] 表に区切り行があり、セル内で改行していない (S-CODE-003)
- [ ] 注意・補足のラベルが4種類のいずれかで、本文が強調の外にある (S-CODE-005)

### 9.5 主張の強さを人手で確認する

- [ ] 曖昧表現に時点・出典・判定基準のいずれかが添えてある (S-VAGUE-001)
- [ ] 「速い」「重い」「安全」に比較対象と測定条件がある (S-VAGUE-001)
- [ ] 制度・法令に関する記述を断定していない (S-VAGUE-001)

## 10. コマンド

```bash
pnpm run validate:style          # ルールを検査する
pnpm run test:style              # 検査スクリプトの回帰テスト
pnpm run generate:glossary       # GLOSSARY.md を再生成する
pnpm run generate:glossary:check # GLOSSARY.md の差分を検出する
pnpm run apply:style-fixes       # 機械適用できるルールを一括適用する
pnpm run report:style-backlog    # STYLE_BACKLOG.md を再生成する
```

`apply:style-fixes` は冪等である。本文と正本JSONの両方を書き換えるため、実行後は `apply:chapter-guides`、`apply:exercise-rubrics`、`generate:exercise-catalog`、`generate:handbook` を続けて実行し、生成物を追従させる。
