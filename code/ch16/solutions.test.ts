import test from 'node:test'; import assert from 'node:assert/strict'; import { execFileSync } from 'node:child_process';
import { InvertedIndex } from './inverted-index.solution.js'; import { BM25Scorer } from './bm25.solution.js'; import { Trie } from './trie-autocomplete.solution.js'; import { cosineSimilarity, VectorStore } from './vector-search.solution.js';
test('inverted index supports AND and phrase search',()=>{const i=new InvertedIndex();i.addDocument(1,'the quick brown fox');i.addDocument(2,'quick fox brown');assert.deepEqual(i.search('quick brown'),[1,2]);assert.deepEqual(i.phraseSearch('quick brown'),[1]);});
test('BM25 ranks relevant documents first',()=>{const s=new BM25Scorer();s.indexDocument(1,'machine learning algorithms');s.indexDocument(2,'deep learning is a subset of machine learning');s.indexDocument(3,'neural networks');assert.equal(s.search('machine learning')[0]?.docId,1);});
test('trie returns weighted suggestions',()=>{const t=new Trie();t.insert('apple',100);t.insert('application',80);t.insert('apply',50);assert.deepEqual(t.suggest('app').map(x=>x.word),['apple','application','apply']);});
test('vector store ranks cosine similarity',()=>{assert.equal(cosineSimilarity([1,0],[1,0]),1);const s=new VectorStore<string>(2);s.add(1,[1,0],'x');s.add(2,[0,1],'y');assert.equal(s.search([.9,.1])[0]?.id,1);});
test('integrated search self-test succeeds',()=>{const out=execFileSync('bash',['mini-search-engine/solution/main.sh','--self-test'],{cwd:new URL(import.meta.url.includes('/dist/')?'../':'./',import.meta.url),encoding:'utf8'});assert.match(out,/Search ranking/);});
