export type Detection={risk:'low'|'medium'|'high';score:number;reasons:string[];sanitized:string};
const RULES=[{re:/ignore (all |the )?(previous|prior) instructions?/gi,score:5,reason:'instruction override'},{re:/(reveal|print|show).{0,30}(system|developer) prompt/gi,score:5,reason:'prompt exfiltration'},{re:/(<<<\s*system\s*>>>|\[system\]|admin mode)/gi,score:4,reason:'role injection'},{re:/(fetch|open|read).{0,30}(https?:\/\/|file:\/\/|\/etc\/)/gi,score:3,reason:'external resource request'},{re:/(do not follow|disregard).{0,30}(policy|rules|safety)/gi,score:4,reason:'policy bypass'}];
export class PromptInjectionDetector{detect(input:string):Detection{let score=0;const reasons:string[]=[];let sanitized=input;for(const rule of RULES){rule.re.lastIndex=0;if(rule.re.test(input)){score+=rule.score;reasons.push(rule.reason);rule.re.lastIndex=0;sanitized=sanitized.replace(rule.re,'[REMOVED]');}}return{risk:score>=5?'high':score>=3?'medium':'low',score,reasons,sanitized};}}
export const exerciseId='29.3';
