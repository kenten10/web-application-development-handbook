# 模範解答 — SQLインジェクション

この演習はローカルの文字列評価シミュレータだけを使用し、実データベースや公開ネットワークへ攻撃文字列を送信しません。

```bash
node --test dist/solutions.test.js
```

`demo.ts` の `unsafeQuery()` は脆弱な文字列連結を再現し、`safeQuery()` はSQLと値を分離します。攻撃入力は隔離されたテストデータにだけ適用してください。
