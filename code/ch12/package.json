{
  "name": "@handbook/ch12",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=24.18.0 <25"
  },
  "scripts": {
    "lint": "node ../../scripts/validate-exercises.mjs --chapter ch12",
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "test": "node --test --import tsx solutions.test.ts && bash sse-push/solution/main.sh >/dev/null && node ../../scripts/validate-exercises.mjs --chapter ch12",
    "build": "tsc -p tsconfig.json --outDir dist"
  },
  "devDependencies": {
    "@types/node": "catalog:",
    "typescript": "catalog:",
    "tsx": "catalog:"
  }
}
