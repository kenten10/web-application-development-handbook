# 模範解答 — 課題2.5

ローカルHTTPサーバを起動し、次の条件を同一プロセス内で比較します。

```bash
COUNT=100 bash code/ch02/antipatterns/solution/main.sh
```

- 小さなレスポンスを多数取得する場合と、1レスポンスへまとめた場合
- 接続を毎回作る場合と、Keep-Aliveを利用する場合
- 大きなJSONをそのまま送る場合と、gzipする場合

結果はOS・CPU・Node.jsバージョンで変わります。速度の絶対値ではなく、接続確立回数・リクエスト数・転送バイト数という原因と対応付けて説明します。
