# 正誤表

このファイルは、公開済みの版に含まれる誤りと、その訂正内容を記録する。

対象は本文、図表、サンプルコード、演習カード、生成された目次・索引・学習ガイドである。誤りを見つけた場合の報告方法は第4節を参照する。

書式と項目の正本は [`config/release.json`](./config/release.json) の `errata` である。`pnpm run validate:release-policy` が、表の列、状態、重大度、IDの書式、および参照している版が [`CHANGELOG.md`](./CHANGELOG.md) に存在することを検証する。

## 1. 登録件数

| 状態 | 件数 |
|---|---:|
| 受付 | 0 |
| 再現確認中 | 0 |
| 確認済 | 0 |
| 修正済 | 0 |
| 却下 | 0 |

## 2. 正誤一覧

| ID | 報告日 | 対象版 | 対象箇所 | 誤 | 正 | 重大度 | 状態 | 修正版 |
|---|---|---|---|---|---|---|---|---|

現在、登録された正誤は0件である。v1.0.0の公開後に受け付けた報告をここへ追記する。

記入例（実際の登録ではない）を示す。

```text
| E-2026-001 | 2026-09-05 | 1.0.0 | 3.4 節 本文3段落目 | TLS 1.2 は 2024 年に非推奨化された | TLS 1.0 と 1.1 は 2021 年に非推奨化された | Major | 修正済 | 1.0.1 |
```

## 3. 記入規則

### 3.1 ID

`E-<報告年>-<3桁連番>` とする。連番は年ごとに `001` から採番し、却下した項目の番号も再利用しない。

### 3.2 対象版

誤りが存在する版のうち、**最も古い版**を書く。複数版にまたがる場合は「1.0.0以降」のように範囲で書かず、最も古い版だけを書く。修正版の欄で訂正が入った版が分かるため、範囲は不要である。

対象版は `CHANGELOG.md` に存在する版番号でなければならない。

### 3.3 対象箇所

節番号または演習IDと、その中の位置を書く。位置は「本文3段落目」「表2の3行目」「コード例12行目」のように、版が変わっても追える粒度で書く。ファイル名と行番号だけでは、本文が動いたときに追えなくなるため使わない。

### 3.4 誤・正

原文をそのまま引用し、訂正後の文をそのまま書く。「〜が誤り」といった要約ではなく、読者が自分の手元の版を機械的に読み替えられる形にする。

### 3.5 重大度

| 重大度 | 定義 | 対応 |
|---|---|---|
| Blocker | 記述どおりに実行すると、データ喪失・情報漏洩・課金事故が起きる | 30日以内にPATCHを発行する |
| Major | 技術的に誤っており、読者の理解が実務で通用しない結論に至る | 次のPATCHで修正する |
| Minor | 誤りだが、前後の文脈から正しい意味を推測できる | 次のMINORまでに修正する |
| Trivial | 誤字脱字、表記ゆれ、リンク切れ | まとめて次の版で修正する |

### 3.6 状態

`受付` → `再現確認中` → `確認済` → `修正済` の順に遷移する。再現しない、または誤りではないと判断した場合は `却下` とし、対象箇所欄に判断理由を追記する。却下した行も削除しない。

## 4. 報告方法

GitHubのIssueテンプレートから報告する。

| 種類 | テンプレート | 用途 |
|---|---|---|
| 正誤報告 | `.github/ISSUE_TEMPLATE/errata-report.yml` | 誤字、技術的な誤り、コードが動かない、リンク切れ |
| 改訂・利用の提案 | `.github/ISSUE_TEMPLATE/revision-proposal.yml` | 記述の追加・変更提案、営利利用の照会、新しい技術動向の反映提案 |

報告に含める情報は、テンプレートの必須項目に従う。読者の氏名、メールアドレス、所属は収集しない（[`BETA_REVIEW_PLAN.md`](./BETA_REVIEW_PLAN.md) 第10節の個人情報方針と同じ扱いとする）。

## 5. 受付から反映までの手順

1. **受付** — Issueが作成されると `errata` ラベルが自動付与される。メンテナは7日以内に受領を返信し、本ファイルへ状態 `受付` で1行追加する。IDはこの時点で採番する。
2. **再現確認** — 本文の指摘は該当箇所を確認し、コードの指摘は固定ツールチェーンで再現を試みる。状態を `再現確認中` にする。
3. **判定** — 誤りと確認できたら重大度を確定して状態を `確認済` にする。誤りでない場合は `却下` にし、理由を記入してIssueを閉じる。
4. **修正** — 本文またはコードを修正するPull Requestを作成する。PRの説明にERRATA IDを書く。
5. **検証** — `pnpm run check:handbook` と該当章の `lint`・`typecheck`・`test`・`build` が成功することを確認する。
6. **反映** — マージ後、`CHANGELOG.md` の `## [Unreleased]` の `Fixed` へ、ERRATA IDを併記して追記する。
7. **リリース** — PATCH版を発行する。本ファイルの状態を `修正済` にし、修正版の欄へ版番号を書き、第1節の件数表を更新する。Issueを閉じる。

Blockerは30日以内、Majorは次のPATCH、Minorは次のMINORまでに反映する。期限を守れない場合は、その理由をIssueへ記録する。

## 6. 版とのつながり

- 正誤の受付だけでは版番号は上がらない。本文またはコードへ反映した時点でPATCHを発行する。
- 1つのPATCHで複数の正誤を反映してよい。その場合、`CHANGELOG.md` の `Fixed` にすべてのIDを列挙する。
- 誤りの訂正が節の骨子を変える場合は、PATCHではなくMINORとする。判断基準は [`RELEASE_POLICY.md`](./RELEASE_POLICY.md) の第3節による。
