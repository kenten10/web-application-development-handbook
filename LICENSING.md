# ライセンス対応表

この文書は、本リポジトリのどのファイルにどのライセンスが適用されるかを、ファイル単位で機械的に判定できる形で定める。

機械可読な正本は `config/release.json` の `licensing` である。本文書の対応表は正本から逸脱してはならず、`pnpm run validate:release-policy` が両者の一致を検証する。

## 1. 二重ライセンス

本書は本文とコードで異なるライセンスを採用する。

| 区分 | SPDX識別子 | ライセンス | 全文 | 対象 |
|---|---|---|---|---|
| 本文 | `CC-BY-NC-SA-4.0` | Creative Commons 表示 - 非営利 - 継承 4.0 国際 | [`LICENSE-TEXT`](./LICENSE-TEXT) | 本文Markdown、図表、文章、生成されたHTML |
| コード | `MIT` | MIT License | [`LICENSE`](./LICENSE) | サンプルコード、スクリプト、設定ファイル、CI定義 |

分離の理由は次のとおりである。

- 本文は著者の解説そのものであり、営利目的の再販や改変版の非公開化を防ぐため、非営利かつ継承条件を課す。
- サンプルコードは読者が自分の仕事へ持ち帰って使うためのものであり、条件の少ないMITとする。学習した内容を業務コードへ取り込むときにライセンス条項が障害にならないようにする。

## 2. 判定規則

判定は `config/release.json` の `licensing.rules` を先頭から順に評価し、**最初に一致したルールを適用する**。後続のルールは評価しない。パターンは次の意味を持つ。

- `dir/**` — `dir/` 配下のすべてのファイル（再帰）
- `*.ext` — リポジトリ直下の該当拡張子のファイル
- それ以外 — リポジトリルートからの相対パスの完全一致

| 順序 | パターン | 区分 | 備考 |
|---:|---|---|---|
| 1 | `LICENSE` | 告知 | MITライセンス本文 |
| 2 | `LICENSE-TEXT` | 告知 | CC BY-NC-SA 4.0の適用条件 |
| 3 | `code/**` | コード | 章別サンプルコード。章内のREADMEと設定も含む |
| 4 | `scripts/**` | コード | 生成・検証スクリプト |
| 5 | `config/**` | コード | 機械可読な正本データ |
| 6 | `.github/**` | コード | GitHub Actionsとissueテンプレート |
| 7 | `.devcontainer/**` | コード | 固定環境定義 |
| 8 | `.verification/**` | コード | 検証用の実行成果物とログ |
| 9 | `*.json` | コード | ルート直下の設定・データ |
| 10 | `*.yaml` | コード | pnpm workspaceとlockfile |
| 11 | `*.yml` | コード | ルート直下のYAML設定 |
| 12 | `*.csv` | コード | 検証結果データ |
| 13 | `*.txt` | コード | 検証ログ |
| 14 | `.gitignore` | コード | リポジトリ設定 |
| 15 | `.npmrc` | コード | パッケージマネージャ設定 |
| 16 | `.node-version` | コード | Node.jsバージョン固定 |
| 17 | `.nvmrc` | コード | Node.jsバージョン固定 |
| 18 | `*.md` | 本文 | 本文と運用文書 |

区分「告知」はライセンス本文そのものを指す。告知ファイルは被許諾物ではないため、CC BY-NC-SA 4.0もMITも適用しない。

判定順序から導かれる帰結を明示しておく。

- `code/ch07/README.md` は規則3に一致するため **MIT** である。規則18（`*.md`）はリポジトリ直下のMarkdownにしか到達しない。
- `README.md`、`CONTRIBUTING.md`、`RELEASE_POLICY.md` などルート直下のMarkdownは規則18に一致するため **CC BY-NC-SA 4.0** である。
- `beta-review-scope.json` や `narrative-flow.json` などルート直下のJSONは規則9に一致するため **MIT** である。

## 3. 判定対象外

次のパスは配布物に含まれないため、ライセンス判定の対象外とする。定義は `config/release.json` の `licensing.ignore` にある。

- `node_modules/**`
- `.git/**`
- `dist/**`
- `build/**`
- `coverage/**`
- `tmp/**`
- `**/.DS_Store`

`dist/**` は生成物であり、生成元のライセンスを継承する。`dist/site/` のHTMLは本文Markdownから生成されるため **CC BY-NC-SA 4.0** として配布する。生成された各ページのフッタにライセンス表示を埋め込む。

## 4. 判定コマンド

```bash
pnpm run validate:release-policy
```

この検証は次を確認する。

- `licensing.ignore` に該当しないすべてのファイルが、いずれかの規則に一致すること
- 本文書の対応表と `config/release.json` の規則が、順序を含めて一致すること
- `LICENSE` と `LICENSE-TEXT` が存在し、SPDX識別子に対応する記述を含むこと

一致しないファイルが1件でもあれば終了コード1を返す。新しい種類のファイルを追加した場合は、`config/release.json` の規則と本文書の表を同時に更新する。

## 5. 第三者著作物

本書が引用する仕様、記事、書籍の権利は各権利者に帰属する。引用は出典を明示したうえで、引用の要件を満たす範囲に限る。出典は [`09-references.md`](./09-references.md) にまとめている。

サンプルコードが外部パッケージへ依存する場合、そのパッケージのライセンスは各パッケージの配布物に従う。本書のMITライセンスは依存パッケージへは及ばない。

## 6. 利用時の表示

本文を再利用する場合の表示例を示す。

```text
本ページは 吉田賢典『WEBアプリケーション開発者になれる解説書 ― 基礎から一流までの30章 ―』
(CC BY-NC-SA 4.0) を改変して作成した。
原著: <取得元URL>
ライセンス: https://creativecommons.org/licenses/by-nc-sa/4.0/
```

サンプルコードを再利用する場合は、[`LICENSE`](./LICENSE) の著作権表示と許諾表示を、コードまたは配布物の告知ファイルへ含める。

営利利用の可否と別途許諾の窓口は [`LICENSE-TEXT`](./LICENSE-TEXT) の第4節・第6節を参照する。
