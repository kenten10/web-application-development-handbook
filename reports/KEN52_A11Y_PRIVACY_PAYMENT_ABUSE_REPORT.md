# KEN-52 アクセシビリティ・プライバシー・決済・abuse対策を補完する — 実施レポート

## 目的

本書は、システムの内側（HTTP、API契約、DB、キュー、セキュリティ）と、KEN-51 で補った外側との境界（ファイル、Webhook、メール、外部API）を厚く扱っている。残っていたのは、**システムの外にいる人と制度に対する責任**である。誰が使えるか（アクセシビリティ）、誰の情報をどれだけ持つか（個人データ）、いくら請求するか（決済）、誰の要求を受け付けるか（濫用対策）の4領域は、いずれも技術判断だけでは閉じない。

KEN-52 では、この4領域を実務頻度に応じた優先順位で第6・7・13・14・17・23・25・27・30章へ横断的に統合し、4領域それぞれの失敗を実際に再現できる演習と、法務・専門家への確認先を明示したチェックリストを追加した。WS4（KEN-32）はこれで完結する。

## 4テーマの実務頻度評価と優先順位付け

issue の完了条件が「**実務頻度に応じた優先順位で**既存章へ統合」であるため、4テーマの実務頻度を根拠つきで評価し、優先順位を決めてから配置と分量を決めた。

| 順位 | テーマ | 遭遇する開発者の範囲 | 遭遇する頻度 | 根拠 | 割いた分量 |
|---:|---|---|---|---|---:|
| 1 | アクセシビリティ | UI を書くすべての開発者 | **画面・部品を作るたび** | フォーム、モーダル、一覧、遷移という日常の部品すべてに判断が要る。既存 6.9 は必修25分だが、扱いは「原則と型」までで、フォーカスの行き先・エラーの通知経路・検証手順という**実行段階の判断**が本書のどこにも無かった | 3節・75分 |
| 2 | 濫用対策 | 公開エンドポイントを持つすべてのチーム | **公開する経路を足すたび** | ログイン、登録、パスワード再設定、問い合わせ、検索、公開ページのどれか1つでも持てば対象になる。既存記述は 23.6 の10行と 14.22 のテナント単位トークンバケットに分散し、しかも 13.1 の「固定回数の一律ロックを避けよ」と 23.6 の「メールアドレス単位で5回ロック」が矛盾していた | 2節・50分 |
| 3 | 個人データ | 会員機能を持つすべてのサービス | **データ項目を足すたび＋開示・削除要求のたび** | 実装が退会とエクスポートの2機能に集中しやすく、日常の実装頻度はa11yより低い。ただし所在把握を後回しにすると**あとから作れない**（派生先が増えたあとで削除の伝播を作るのは桁違いに難しい）。既存 28.14 はコード例ゼロの規制サーベイ、22.3 の構造化ログはIPとUAをマスクせず出力する例を GOOD として示していた | 2節・50分 |
| 4 | 決済 | 課金するサービスのみ | **導入時と料金改定時に集中** | 対象サービスが最も限られ、担当も分かれる。ただし失敗コストは最大（相手の資産が動く）。既存 30.8 と 30.13.1 は Stripe 連携の実装例と落とし穴の列挙で、金額の型・丸め・端数配分・返金の上限・突合という**判断**は扱っていなかった | 2節・50分 |

順位は次の3つに反映した。

1. **節数と分量**: 1位は3節、2〜4位は各2節。
2. **配置の広さ**: 1位のa11yだけは3つの部（第II部・第VI部）へ分散させ、部品・状態・検証の各文脈で扱った。
3. **既存章との接続密度**: 1位・2位は既存の必修節（6.9、13.1、23.6）の直系として置き、3位・4位は実務選択節の系列（14.20〜14.24、17.13〜17.15）の続きとして置いた。

なお、**学習レベルは新設10節すべて `practical`（実務選択）** とした。優先順位は分量と配置に反映し、必修節は増やしていない。必修を増やすと `beta-review-scope.json` の `requiredSectionCount`（199）、章別 `requiredMinutes`、選定しきい値の分位点、章の tier が一斉にずれるためである。結果として必修は **199節・24時間5分のまま変わっていない**。

## 横断配置の設計判断

### 章番号は振り直さず、既存章へ節として統合した

書名・目次・学習経路・索引・`code/ch01`〜`code/ch30`・検証スクリプトのすべてに「30章」が埋め込まれている。KEN-49 / KEN-50 / KEN-51 と同じく、新規章（第31章）は作らず既存章へ節として足した。

### どの節をどこへ置いたか

| issue の作業項目 | 配置 | 章を選んだ理由 |
|---|---|---|
| a11y ― フォーカス管理 | **6.11**（新設・フロントエンドフレームワーク） | 「いま入力を受け取る要素は文書内に1つ」という制約は、部品化しても分割されない。6.10 の Shadow DOM が「スタイルとDOMの境界を部品ごとに閉じる」直後に、「閉じても分割されない単一の資源」を置くと対比が立つ。6.9 が示した `role` / `aria-*` は「何であるか」を伝える手段であり、6.11 は「いまどこにいるか」を**状態遷移として設計する**話であって層が違う |
| a11y ― フォームエラー | **7.9**（新設・状態管理とデータフェッチング） | 7.7 が扱ったフォーム状態（入力途中・検証結果・送信状態）を、画面に描くだけでなく**支援技術へ届く形へ変換する**話である。7.8 のスタイリングで見た目を作り込むほど意味が視覚表現へ逃げる、という流れが直結する。第6章ではなく第7章に置いたのは、問題の中心が ARIA 属性ではなく**状態の写し取り**だからである |
| a11y ― 読み上げ検証 | **25.11**（新設・テスト戦略） | 25.10 の「変更時に壊れたら気づきたいもの」にアクセシビリティは含まれるが、25.9 までの技法と違い**自動テストでは判定できない部分が構造的に残る**。何を自動化し、何を手順化し、何を人が確かめるかを分ける判断は、第6章の実装知識ではなくテスト戦略の枠組みに属する。第22〜26章にa11y検証の記述が皆無だったという空白も埋まる |
| 個人情報 ― 収集・保存 | **14.25**（新設・リレーショナルデータベース） | 14.20〜14.22 のテナント境界、14.23〜14.24 の日時と同じく、「取り違えると事故になる軸」をデータストア設計として扱う。個人データはそこに**寿命**という軸が加わる。所在一覧、収集の最小化、ログへの漏れ、閲覧経路の設計はいずれも列とスキーマとクエリの話であり、第28章の規制サーベイ（28.14）とは層が違う |
| 個人情報 ― 削除・エクスポート | **14.26**（新設・同上） | 14.25 の続き。14.21 の crypto-shredding と削除段階表を前提にできる位置にあり、削除の伝播先（検索インデックス、分析基盤、オブジェクトストレージ）がいずれも第IV部で扱った保存場所であるため、参照が最短になる |
| 決済 ― 金額表現 | **27.18**（新設・設計とドメインモデリング） | 27.2 の `Money` は「ただの数値とお金は別物だ」を型で示したが、通貨ごとの小数桁、丸めの時点と方式、端数の配分規則を持たない。金額は Value Object の正典例であり、**ドメインモデリングの章で詰めるのが素直**である。第14章（列型）や第17章（連携）に置くと、モデルの話が実装の話に埋もれる |
| 決済 ― Webhook・二重課金・返金 | **17.16**（新設・イベント駆動とメッセージング） | 17.15 の外部API連携（時間予算・リトライ・ブレーカ）が「相手が決済事業者になると性質が変わる」という形で直結する。取得の失敗は再取得で済むが、課金の失敗は「起きたかどうか分からない」状態を残す。17.13 の Webhook 受信をそのまま前提にでき、重複説明が要らない |
| abuse ― Credential Stuffing・列挙 | **13.25**（新設・認証と認可） | 「正しい資格情報を使う攻撃」には 13.1 のハッシュ強度も 13.10 の認可モデルも効かない。効くのは試行そのものの扱い方であり、これは認証の設計判断である。13.1 が方針だけ4行で述べ、23.6 が矛盾する実装例を出していた**ねじれを解消する位置**でもある |
| abuse ― bot・スパム・レート制限 | **23.27**（新設・セキュリティ） | 23.2〜23.26 が「1回の要求で境界を越えられる」脆弱性なのに対し、ここは個々の要求がすべて正当で、問題が**回数**にある。脅威の分類、判定の段階、CAPTCHA の費用、レート制限の鍵・層・返し方はセキュリティ章の枠組みで扱うのが正しい |
| 正式版の範囲決定と免責 | **30.16**（新設・総合演習） | 4領域に共通するのは「技術判断だけでは答えが決まらない項目を含む」ことである。30.14（内側を貫く前提）、30.15（外側との境界）と対になる3つ目として、**外にいる人と制度に対する責任**を置いた |

### 既存記述との重複を避けた方法

- **6.9 は一切変更していない。** 6.11 は「6.9 の `role` と `aria-*` は何であるかを伝える。ここで扱うのはいまどこにいるかである」と層を明示して接続した。`label` と `aria-describedby` の型（6.9 の「4. フォームのラベルとエラー」）は 7.9 で再掲せず、**名前の供給元の優先順位**と**エラーが載る3経路**という別の切り口から積んだ。
- **23.6 の実装例は残したまま**、13.25 で「23.6 のコードはブルートフォースには効くが、パスワードスプレーと Credential Stuffing には外れる」と明記し、13.1 の方針との矛盾を解消した。既存コードを削っていない。
- **課題23.5（Token Bucket / Sliding Window の自作）と重複させない。** 23.27 は「アルゴリズムは 14.22 と課題23.5 で扱っている」と参照で済ませ、鍵の設計・層の置き方・応答の返し方だけを扱った。課題23.10 も固定窓の最小実装を道具として使うだけである。
- **12.15 / 17.13 の Webhook 署名・冪等性は再説明していない。** 17.16 は「受信側の実装は 17.13 のとおりで、決済でも変わらない」と明示し、決済に固有な**呼び出し側の冪等キーの確保順序**、与信と確定の分離、猶予期間の状態、返金の上限、突合の3方向だけを扱った。26.10（自分たちのAPIが受ける側）、12.15・17.13（Webhook を受ける側）、17.16（自分たちが呼ぶ側）の3層の違いを本文で明示している。
- **28.14 の規制サーベイは残したまま**、14.25 / 14.26 / 30.16 から参照した。14.25 と 14.26 は法令の解説を一切増やさず、「どの制度の下でも共通して必要になる技術的な準備」に限定している。
- **27.2 の `Money` は変更していない。** 27.18 は「27.2 は原則を型で示した。ここではそれを実務で使える形まで詰める」として、通貨の小数桁、丸めの時点と方式、端数配分、為替換算の明示化を足した。
- **22.3 の構造化ログの例も変更していない。** 14.25 で「22.3 で扱った構造化ログは、そのまま使うと個人データの複製装置になる」と受けて、許可リスト方式を提示した。

### 見出しの重複回避

新設9節（30.16 を除く）の `#### つまずく箇所` にはすべて識別子を付けた（`― フォーカス管理`、`― フォームのアクセシビリティ`、`― アクセシビリティの検証`、`― 個人データの収集と保存`、`― 保持期間、削除、エクスポート`、`― 金額と通貨の表現`、`― 決済連携`、`― 認証エンドポイントの濫用`、`― 自動化された脅威`）。30.16 の `#### 使い方` は 30.14 の同名見出しと衝突するため、最初から `#### 使い方 ― 4領域チェックリスト` とした（KEN-51 が 30.15 で `― 外部連携チェックリスト` としたのと同じ扱い）。**結果として `ANCHOR_DUPLICATE` は増えていない。**

## 正式版に含めた範囲／発展資料へ回した範囲

issue の「正式版に含める範囲と発展資料へ回す範囲を決定」に対し、**落とすものは「なぜ落とすか」と「どこを見ればよいか」を本文に明記して落とした**。黙って省略した項目は無い。

### 正式版に含めた

| 領域 | 含めた内容 |
|---|---|
| a11y | フォーカス可能性の3段階と `tabindex`、`:focus-visible` と `forced-colors`、モーダルの4動作と `dialog`／自前実装、`aria-hidden`・`inert`・`display:none` の違い、ルート遷移と動的更新、Tab とロービング `tabindex`、名前の供給元の優先順位、`fieldset`/`legend`、`required`・`autocomplete`・`aria-describedby`、エラーの3経路、ライブリージョンの4つの性質、検証タイミング、`aria-disabled`、自動検査で拾えるもの／拾えないもの、部品単位と画面単位の使い分け、期限付き抑制、キーボード走査の8項目、支援技術で確認する4点、`getByRole` による回帰検知 |
| 個人データ | 所在一覧の列設計、収集しない・保持しない・粒度を落とす・仮名化・暗号化の5段階、分類のスキーマとコードへの埋め込み、ログとトレースの4つの漏れ口と許可リスト方式、内部の閲覧経路の設計、保持期間の3種別と実行、削除の伝播（6段階）と進捗記録と冪等性、消せないものの扱い、バックアップの明文化、エクスポートの範囲判断表と形式、同意の記録項目と撤回の到達、事故時に答えられるべき4項目 |
| 決済 | 最小単位の整数／十進小数／有理数の比較、ISO 4217 の小数桁、API と DB の型、丸めの時点と4方式、端数の配分（合計不変）、為替換算の明示化、カード情報を自前で扱わない方針、タイムアウトが「不明」であること、冪等キーの確保順序、与信と確定・ダニング・申し立ての状態、返金を独立行として持つ設計と上限の直列化、突合の3方向、環境と鍵の分離 |
| 濫用対策 | 4種類の攻撃と効く対策の対応、鍵の5層、段階的な費用の引き上げ、成功後の合図と漏洩資格情報の照合（k-匿名性）、列挙対策の6経路、ATO 後の手順、自動化された要求の4分類、判定の3段階、チャレンジ6方式の費用比較とアクセシビリティ、スパムを経路で減らす、レート制限の鍵・層・返し方、`429` と `Retry-After` と `RateLimit` 系ヘッダ、検知モード、拒否と巻き添えの両測定、分散カウンタ |

### 発展資料・将来版へ回した（理由と参照先を本文に明記）

| 落としたもの | なぜ落とすか | どこを見ればよいか（本文での記述） |
|---|---|---|
| WCAG の達成基準を1つずつ解説すること | 基準の網羅は仕様書の役割であり、判断の説明にならない。版によって基準も番号も変わる | 6.9 と 25.11 から WCAG 2.2 [W3C WCAG, 2024]、WAI-ARIA [W3C ARIA, 2023]、APG [W3C APG] へ誘導 |
| 複合ウィジェットごとの実装（コンボボックス、ツリー、グリッド） | 部品ごとに数十ページ必要で、正しい実装は APG に網羅されている。本書は「Tab はウィジェット間、矢印はウィジェット内」という**判断の型**までを扱う | 6.11 の表と [W3C APG] |
| 特定の支援技術の操作方法 | 組み合わせごとに挙動が違い、版で変わる。開発者が不慣れなまま判断すると実装を壊す | 25.11 で「確認する4点」に絞り、日常はアクセシビリティツリーを使うと明記 |
| CCPA/CPRA・HIPAA・SOC 2 の詳細 | 28.14 の概観で足り、要件の解釈は本書の範囲外 | 30.16 の免責表と 28.14 |
| k-匿名化・差分プライバシー | 統計的開示制御は独立した分野で、Webアプリの実装判断とは粒度が違う | 14.25 で仮名化と粒度低下までを扱い、それ以上は扱わないと明示 |
| Cookie 同意バナー／CMP の実装 | 実装の中身より「同意の記録と撤回の到達」が本質で、UI は製品ごとに違う | 14.26 の同意の記録項目と撤回の経路 |
| 越境移転の技術実装（データレジデンシー、SCC） | 契約と法域の判断が支配的で、技術判断が従属する | 30.16 の免責表（確認先: 法務） |
| 税計算・収益認識・プロレーション | 制度と会計の領域であり、決済連携の技術判断とは別 | 17.16 で「制度が決める」と明示、30.13.1 の Stripe Tax 等の記述へ接続 |
| 決済事業者ごとの API の差 | 事業者と版で変わる。本書の版管理より速い | 17.16 で「事業者ごとに異なる」と明示、[PCI DSS] と事業者資料へ誘導 |
| WAF ルールセットの運用（OWASP CRS の調整） | 製品固有かつ運用の比重が大きい | 23.27 で「検知モードから始める」という判断だけを扱う |
| L3/L4 volumetric DDoS の対処 | アプリケーションの実装では対処できない領域 | 23.27 の末尾で「上流の事業者に委ねる」とし、アプリ側で用意する2点だけを明記 |
| bot 判定サービスの比較 | 事業者選定であり、かつ第三者へのデータ提供というプライバシー判断を伴う | 23.27 のチャレンジ比較表に「第三者へ利用者の情報が渡る」と明記 |

## 追加した節・小節の一覧と分量

| 節 | 学習レベル | 推定時間 | 行数 | 文字数 | 主な小節 |
|---|---|---:|---:|---:|---|
| 6.11 フォーカス管理 ― モーダル、動的更新、ルート遷移 | 実務選択 | 25分 | 205 | 8,539 | 3段階のフォーカス可能性／`:focus-visible` と `forced-colors`／モーダルの4動作／自前トラップの限界／`aria-hidden`・`inert`・`display:none`／ルート遷移と一覧削除／Tab と矢印の分担／つまずく箇所 |
| 7.9 フォームのアクセシビリティ ― 名前、エラー通知、送信の結果 | 実務選択 | 25分 | 161 | 6,798 | 名前の供給元と優先順位／`fieldset`・`legend`／必須と形式の事前提示／エラーの3経路／エラーサマリの実装／ライブリージョンの4性質／検証の時機／`aria-disabled`／つまずく箇所 |
| 13.25 認証エンドポイントの濫用 ― Credential Stuffing とアカウント列挙 | 実務選択 | 25分 | 130 | 5,696 | 4種類の攻撃と効く対策／鍵を層として置く／段階的な費用の引き上げ／成功時の合図と漏洩資格情報の照合／列挙対策の6経路／ATO 後の手順／つまずく箇所 |
| 14.25 個人データの収集と保存 ― 何を持ち、どこに置き、どこへ漏れるか | 実務選択 | 25分 | 142 | 6,057 | 所在一覧／持たないことが最良／列単位の分類と型／ログとトレースの漏れ口と許可リスト／内部の閲覧経路／暗号化の限界／つまずく箇所 |
| 14.26 保持期間、削除、エクスポート ― 個人データの終わり方 | 実務選択 | 25分 | 167 | 6,669 | 4つの要求と必要な仕組み／保持期間の3種別／削除の伝播と4つの要点／エクスポートの2種類の事故／同意と目的の記録／事故時に答えられる状態／つまずく箇所 |
| 17.16 決済連携の実務 ― 二重課金、返金、突合 | 実務選択 | 25分 | 205 | 8,487 | カード情報を自前で扱わない／不明という状態と冪等キー／状態を事業者から受け取る／返金は別の取引／突合の3方向／環境と鍵の分離／つまずく箇所 |
| 23.27 自動化された脅威 ― bot、スパム、レート制限の設計 | 実務選択 | 25分 | 149 | 6,597 | bot を意図で分ける／判定の3段階／チャレンジ6方式の費用とアクセシビリティ／スパムを経路で減らす／鍵・層・返し方／拒否と巻き添えの両測定／大量要求への備え／つまずく箇所 |
| 25.11 アクセシビリティの検証 ― 自動チェック、キーボード走査、読み上げ確認 | 実務選択 | 25分 | 160 | 6,213 | 3層モデル／拾えるもの拾えないもの／部品単位と画面単位／期限付き抑制／キーボード走査の8項目／支援技術で確認する4点／`getByRole` による回帰検知／直せないものの扱い／つまずく箇所 |
| 27.18 金額と通貨の表現 ― 最小単位、丸め、配分 | 実務選択 | 25分 | 195 | 7,499 | 浮動小数点数を使わない／ISO 4217 の小数桁／丸めの時点と4方式／端数の配分／通貨を混ぜない／表示と計算の分離／つまずく箇所 |
| 30.16 アクセシビリティ・個人データ・決済・濫用対策のチェックリストと免責 | 実務選択 | 15分 | 181 | 6,936 | 免責と確認先（8項目）／一次資料の起点／A. a11y(29項目)／B. 個人データ(25項目)／C. 決済(18項目)／D. 濫用対策(27項目)／計99項目／使い方 |
| 課題6.6 の本文（6.12 内） | — | — | 43 | 2,030 | 題材、4つの実装要件、誤りの表、評価基準、期待出力 |
| 課題14.7 の本文（14.27 内） | — | — | 43 | 2,160 | 同上 |
| 課題17.7 の本文（17.17 内） | — | — | 44 | 1,979 | 同上 |
| 課題23.10 の本文（23.28 内） | — | — | 42 | 1,887 | 同上 |
| 合計 | — | **240分** | **1,867** | **77,547** | — |

各節はコード例（TypeScript、TSX、SQL、CSS、HTTP、JSON）、表、ASCII図、実務での落とし穴、一次資料への出典を備えている。行数あたりの推定時間は既存節と同水準に揃えた。

### 章番号・節番号

- 章数は 30 のまま。新規章は作っていない。
- 実装課題の節番号を9件だけ移動した（6.11→6.12、7.9→7.10、13.25→13.26、14.25→14.27、17.16→17.17、23.27→23.28、25.11→25.12、27.18→27.19、30.16→30.17）。他章の節番号は変更していない。
- 節数は **405 → 415**。
- 実装課題節の推定時間を演習追加分だけ調整した（6.12: 300→450分、14.27: 360→510分、17.17: 580→730分、23.28: 450→600分。他5節は据え置き）。
- 第VII部の総括（`## まとめ ― 第VII部の総括`）が 30.15 と 30.16(旧) の間にあったため、新設 30.16 の後ろ・30.17 の前へ移動し、本文に 30.16 の位置づけを追記した。

## 追加した演習

| 番号 | タイトル | 難易度 | 推定時間 | 環境区分 | starter | solution |
|---|---|---|---:|---|---|---|
| 6.6 | 課題6.6: フォーカスとエラー通知の欠落を再現して塞ぐ (★★★) | ★★★ | 150分 | local-automated | `code/ch06/a11y-focus/starter/main.ts`（実行入口 `starter/report.ts`） | `code/ch06/a11y-focus/solution/main.ts`、`solution/report.ts` |
| 14.7 | 課題14.7: 個人データの削除・保持・エクスポート・同意の抜けを再現して塞ぐ (★★★) | ★★★ | 150分 | local-automated | `code/ch14/data-lifecycle/starter/main.ts` | `code/ch14/data-lifecycle/solution/main.ts`、`solution/report.ts` |
| 17.7 | 課題17.7: 決済連携の二重課金・返金超過・突合欠如を再現して塞ぐ (★★★) | ★★★ | 150分 | local-automated | `code/ch17/payment-integration/starter/main.ts` | `code/ch17/payment-integration/solution/main.ts`、`solution/report.ts` |
| 23.10 | 課題23.10: 濫用対策の鍵と応答の設計を破って塞ぐ (★★★) | ★★★ | 150分 | local-automated | `code/ch23/abuse-defense/starter/main.ts` | `code/ch23/abuse-defense/solution/main.ts`、`solution/report.ts` |

4件とも `local-automated` にした。実ブラウザ・支援技術・決済事業者・攻撃対象ホストを必須にすると `clean-environment-plan.json` の `environmentDependent` が増え、`validate-beta-review.mjs` の選定基準 C3 と必須検証演習 E1 に該当して `beta-review-scope.json` と KEN-60 の計画文書を書き換える必要が生じる。学習目的（フォーカス順序の規則、エラーの到達経路、所在一覧の伝播、保持期間の実行、エクスポートの範囲、同意の到達、冪等キーの確保順序、返金の上限、突合の3方向、鍵の層と応答の統一）はいずれもプロセス内で同じ精度で観察できる。

### 課題6.6 が再現するもの

`El`（id・タグ・属性・子）からなる最小の文書モデルと `Screen`（`activeId` とライブリージョンの履歴）を使い、`naive` と `fixed` の2つのダイアログ／フォーム実装へ同じ検査を当てる。

| 番号 | 誤り | 再現される事象 |
|---|---|---|
| A1 `focus-not-moved` | 開いてもフォーカスを中へ移さない | 押したボタンにフォーカスが残り、ダイアログの内容へ到達できない |
| A2 `focus-escapes` | 背後を `aria-hidden` にするだけで `inert` にしない | Tab が背後の6要素へ抜ける |
| A3 `focus-not-restored` | 閉じたときの戻し先を決めていない | 削除の確認で戻り先が消え、フォーカスが `body` へ落ちる |
| A4 `error-not-announced` | エラーを描画するだけで、どの経路にも載せない | 送信ボタンにフォーカスがある利用者へ何も届かない |

実行結果:

```
naive ui: 4/4 barriers reproduced
  A1 focus-not-moved: naive focus=del-2 inside-dialog=false / fixed focus=confirm inside-dialog=true
  A2 focus-escapes: naive escaped=6 first=del-3 / fixed escaped=0 first=none
  A3 focus-not-restored: naive after-close=(body) / fixed after-close=page-title
  A4 error-not-announced: naive delivered=false / fixed delivered=true
fixed ui: 0/4 barriers remaining (valid submit still announced)
```

最終行の `valid submit still announced` は、`fixed` が正しい入力まで拒んでいないことの確認である。

### 課題14.7 が再現するもの

9か所の保存場所を持つ `World` と、場所ごとの保持期間・エクスポート可否・削除のしかたを持つ `INVENTORY` を使う。判定は固定の基準時刻に基づき、現在時刻に依存しない。

| 番号 | 誤り | 再現される事象 |
|---|---|---|
| P1 `delete-not-propagated` | 主テーブルと外部キーで辿れる範囲だけを消す | 検索インデックス・分析基盤・ストレージ・外部SaaS・ログ・監査ログに10行が残る |
| P2 `retention-not-enforced` | 保持期間を文書にだけ書き、実行するものが無い | 期限を過ぎた4行が残り続ける |
| P3 `export-includes-others` | 参加スレッドを丸ごと書き出す | 他人が書いた連絡先を含む1行が本人向けの書き出しに混ざる |
| P4 `consent-revoked-but-sent` | 配信バッチが同意の正本を見ていない | 撤回済みの主体へ配信が続く |

実行結果:

```
naive lifecycle: 4/4 gaps reproduced
  P1 delete-not-propagated: naive residual=10 at=[analytics.events, db.audit_log, log.requests, saas.crm, search.users, storage.uploads] / fixed residual=0 at=[]
  P2 retention-not-enforced: naive expired-left=4 purged=0 / fixed expired-left=0 purged=4
  P3 export-includes-others: naive rows=6 foreign=1 missing=0 / fixed rows=9 foreign=0 missing=0
  P4 consent-revoked-but-sent: naive sent=[S1, S2] / fixed sent=[S2]
fixed lifecycle: 0/4 gaps remaining (other subject intact)
```

`db.orders` と `db.audit_log` は行ごと消さず識別項目だけを落とす扱いにしてある。取引記録と監査ログを消してよいかは制度と業務が決めるため、コード側は一覧から受け取った種別に従うだけ、という構造にしている。既存の課題28.4（PIIスキャナ + 削除キット）とは扱う対象が違う（28.4 は検出とマスキング、14.7 は伝播・保持・範囲・同意）。

### 課題17.7 が再現するもの

`FakeGateway`（冪等キーによる取引の同一視、「相手には届くが応答が返らない」状態、返金上限、記録の一覧取得だけを持つメモリ上の模擬）を使う。実在の鍵・エンドポイント・カード情報は含まない。

| 番号 | 誤り | 再現される事象 |
|---|---|---|
| M1 `float-money-split` | 主単位の浮動小数点数で按分する | 1,000円を3人へ分けた合計が 999円 になる |
| M2 `double-charge` | タイムアウトを失敗として扱い、新しい鍵で作り直す | 1件の注文に対し事業者側へ2件の取引ができる |
| M3 `refund-exceeds-capture` | 上限の検査と反映が別の操作に分かれている | 6,000円の部分返金が2本通り、累計が 12,000円 になる |
| M4 `reconcile-gap` | 突合を持たない | 届かなかった通知1件が永久に見つからない |

実行結果:

```
naive payments: 4/4 defects reproduced
  M1 float-money-split: naive sum=999 expected=1000 / fixed sum=1000 expected=1000
  M2 double-charge: naive charges=2 / fixed charges=1
  M3 refund-exceeds-capture: naive refunded=12000 capture=10000 / fixed refunded=6000 capture=10000
  M4 reconcile-gap: naive detected=0 / fixed detected=1
fixed payments: 0/4 defects remaining (legitimate charge and refund still pass)
```

M3 は「2本の要求が両方とも検査を通ってから、両方とも書く」という順序を明示的に組み立てて再現している。逐次に実行すれば2本目は上限で止まるため、この交錯を作らないと再現しない。既存の課題26.4（冪等性キー）は自分たちのAPIが受ける側、KEN-51 の課題17.5 は Webhook を受ける側、本課題は**自分たちが呼ぶ側**を扱う。

### 課題23.10 が再現するもの

資格情報も漏洩リストもすべて架空の値で、攻撃の模擬はプロセス内で完結する。

| 番号 | 誤り | 再現される事象 |
|---|---|---|
| B1 `credential-stuffing` | 鍵がアカウント単位だけで、漏洩資格情報を照合しない | 1アカウント1回の分散攻撃で2件が認証を通る |
| B2 `user-enumeration` | 文言を分け、ハッシュ検証も省く | 文言と処理の有無の2つからアカウントの存在が判別できる |
| B3 `lockout-dos` | 固定時間ロックする | 攻撃者が正規の利用者を締め出せる |
| B4 `missing-retry-after` | 拒否に `Retry-After` を付けない | クライアントが待つべき時間を知れない |

実行結果:

```
naive guard: 4/4 weaknesses reproduced
  B1 credential-stuffing: naive accepted=2 [chiba@example.test, bito@example.test] / layered accepted=0 []
  B2 user-enumeration: naive distinguishable=2/2 / layered distinguishable=0/2
  B3 lockout-dos: naive victim-blocked=true / layered victim-blocked=false
  B4 missing-retry-after: naive blocked=25/30 status=429 retry-after=none / layered blocked=10/30 status=429 retry-after=30
layered guard: 0/4 weaknesses remaining (normal login still succeeds)
```

B3 の判定では「追加確認を求められること」を締め出しとは数えていない。本人が越えられる関門が残っているかどうかが、固定ロックとの違いだからである。課題23.5（Token Bucket / Sliding Window の自作）とは扱う層が違い、本課題は固定窓を道具として使うだけである。

### 演習カードの必須フィールド

KEN-48 で定義された全項目を4件に揃えた（目的、難易度、推定時間、推定時間の内訳、前提4件、完成条件6件、期待出力5件、観察項目5件、テスト方法4件、段階的ヒント3段、本番利用時の警告3〜4件、starter/solution の導線）。`validate:exercises` が欠落・定型文・導線不一致・★数不一致を検出しないことを確認している。

**観察項目は、実際にコードを書き換えて挙動を確認し、記載どおりの結果になることを検証した。検証で記載と食い違った項目は、実測結果に合わせて書き直している。** 当初案のうち5件（6.6 の2件、14.7 の1件、17.7 の1件、23.10 の1件）は「書き換えても出力が変わらない」ことが実測で判明したため、別の観察項目へ差し替えた。

| 書き換え | 実測結果 | 採否 |
|---|---|---|
| `fixedDialog.open` から `inert` を外す | A2 だけが再現に戻る（fixed 1/4、`escaped=5`） | 採用 |
| `naiveDialog.open` へ `inert` だけを足す | A2 は解消するが A1 が残る（naive 3/4、`escaped=0`） | 採用 |
| `naiveDialog.open` へフォーカス移動だけを足す | A1 は解消するが A2 が残る（naive 3/4、`escaped=5`） | 採用 |
| `focusAfterDelete` の `deleteItem` を外す | fixed の `after-close` が `del-2` になる（戻り先が生きていれば元へ戻る） | 採用 |
| `fixedForm` のフォーカス移動先を `submit` にする | A4 だけが再現に戻る（fixed 1/4） | 採用 |
| `close` の `focusable` 判定を外す | **変化なし**（消えた要素は `find` が返さず、結局 fallback へ行くため） | 差し替え |
| `tabbables` の負 `tabindex` 判定を外す | **変化なし**（report の4件に影響しない） | 差し替え |
| `INVENTORY` から `search.users` を外す | P1 が再現に戻る（`residual=1 at=[search.users]`） | 採用 |
| `db.orders` の `erasure` を `retain` にする | P1 が再現に戻る（`residual=1 at=[db.orders]`） | 採用 |
| `fixedPurge` の既定 `limit` を 2 にする | P2 が再現に戻る（`expired-left=2 purged=2`） | 採用 |
| `fixedExport` をスレッド単位へ戻す | P3 が再現に戻る（`foreign=1`） | 採用 |
| `fixedDispatch` から `consentActive` を外す | P4 が再現に戻る（`sent=[S1, S2]`） | 採用 |
| `db.audit_log` の `erasure` を `delete` にする | **report は変化なし**（章テストでのみ検出される） | 差し替え |
| `allocate` の余りの配り直しを外す | M1 が再現に戻る（fixed `sum=999`） | 採用 |
| `fixedCharge` の鍵に試行回数を混ぜる | M2 が再現に戻る（fixed `charges=2`） | 採用 |
| `tryAddRefund` の上限検査を外す | M3 が再現に戻る（fixed `refunded=12000`） | 採用 |
| `reconcile` の `missing-local` を外す | M4 が再現に戻る（fixed `detected=0`） | 採用 |
| `FIXTURES.partialRefund` を 5,000 へ下げる | M3 が naive でも再現しなくなる（`refunded=10000` で上限ちょうど） | 採用 |
| `MINOR_UNITS` の JPY を 2 にする | **変化なし**（`naiveAllocate` の丸め結果が同じ） | 差し替え |
| `signals` をアカウント1層だけにする | B4 が再現に戻る（`blocked=0/30 status=401 retry-after=none`） | 採用 |
| `BREACHED` の照合を外す | B1 が再現に戻る（layered `accepted=2`） | 採用 |
| 応答文言を成否で分ける | B2 が再現に戻る（`distinguishable=1/2`） | 採用 |
| 429 の条件を `>= 1` にする | 4件は解消したまま、拒否件数が 10→25 へ増える（巻き添えとの連動） | 採用 |
| `normalizeEmail` を外す | report は変化なし。章テストが `401` を返して失敗する | 採用（章テスト側の観察として明記） |

### 章テストへの追加

- `code/ch06/solutions.test.ts` へ4件追加（4件 → 8件）: 4件の再現と解消、`inert` と `aria-hidden` の差、閉じたあとの復帰と代替、エラーの到達経路とアクセシブルな名前。
- `code/ch14/solutions.test.ts` へ6件追加（6件 → 12件）: 4件の再現と解消、一覧に沿った削除と冪等性、未対応の場所での例外、上限つき purge、エクスポートの範囲、撤回の反映。
- `code/ch17/solutions.test.ts` へ5件追加（8件 → 13件）: 4件の再現と解消、配分の合計不変、冪等キーの確保順序、返金の上限と重複ID、突合の3方向。
- `code/ch23/solutions.test.ts` へ4件追加（11件 → 15件）: 4件の再現と解消、層を重ねた鍵、応答の統一と締め出しの不在、`429` と `Retry-After` と鍵の正規化。

## セキュリティ・法務上の免責と確認先をどこにどう書いたか

**免責は1か所にまとめず、判断が必要になる場所の直前に置いた。** 読者が該当箇所だけを読んでも免責に出会う形にするためである。

| 場所 | 書いたこと |
|---|---|
| **30.16 冒頭「本書がここで扱えないこと ― 免責と確認先」** | 「本節を含め、本書は法的助言ではない」と明記したうえで、**8つの判断**（適用法域、取り扱いの根拠、「削除しました」の範囲、保存義務、a11y の適合表明、PCI DSS の範囲、決済の許認可、攻撃者への対処）について「なぜ本書で決められないか」と「確認先」を表にした。さらに一次資料の起点（WCAG 2.2 / WAI-ARIA / APG / GDPR 条文 / 個人情報保護委員会ガイドライン / PCI DSS / ISO 4217 / OWASP）を別表で示し、「本書の要約ではなく原文にあたるためのもの」と位置づけた |
| **同・末尾** | 「本書の記述が『〜が一般的である』という形になっている箇所は、制度によって結論が変わるため断定を避けている。そのような箇所を自分たちの状況での結論として読まない」と明記 |
| **同・使い方** | 「免責の表で挙げた判断に該当する項目は、チェックを付ける前に確認先を決め、いつ誰に確認したかを記録する」 |
| **14.25 冒頭** | 「本節は法的助言ではない」。何が個人データにあたるか、どの取り扱いにどの根拠が要るかは法域・業種・契約で変わること、扱うのは「どの制度の下でも共通して必要になる技術的な準備」に限ることを明記 |
| **14.26 冒頭** | 「繰り返すが、これは法的助言ではない」。応じる義務・期間・拒否可能性・本人確認の水準は法域と契約で異なると明記 |
| **14.25 本文** | 仮名化と匿名化の区別について「この判断は自分たちだけで結論を出さない」 |
| **14.26 本文** | 保存義務と削除要求の衝突、監査ログの扱い、バックアップの説明範囲、クリプトシュレッディングの制度上の評価について、いずれも「開発側で結論を出さない／自分たちで結論を出さない」 |
| **14.26 同意の項** | 「同意はあらゆる取り扱いの万能な根拠ではない。『とりあえず同意を取ればよい』という設計にはしない」 |
| **17.16 冒頭** | 「本節は法的助言でも、決済事業に関する規制の解説でもない」。カード情報、決済代行契約、資金移動・前払式支払手段の規制、税は法域と契約で大きく異なると明記。扱うのは「技術的に何を保証し、何を保証できないか」に限る |
| **17.16 PCI DSS の項** | 「どこまでが対象範囲かは自分たちで結論を出さない。決済事業者が提供する資料と、必要なら評価機関に確認する」 |
| **25.11 末尾** | 「適合レベル（WCAG のA / AA など）を満たしたと外部へ表明するかどうかは、技術的な判断だけでは決まらない」。表明の形式・必要な根拠・法的位置づけは国・地域・業種・調達要件で異なること、扱うのは「実装が意図どおり動いているかを開発者が確かめる方法」に限ることを明記 |
| **6.9（既存）との整合** | 6.9 が既に「適用法と達成基準は国・地域・業種で異なるため、実案件では法務・アクセシビリティ専門家と確認する」と述べており、25.11 と 30.16 はこれを受ける形にした |
| **演習カードの警告** | 4件すべてに法務・専門家への確認を明記。課題14.7 は「本番のデータベース、レプリカ、バックアップに対して実行してはならない。削除は元へ戻せない」、課題17.7 は「必ずテスト環境の鍵だけを使う」、課題23.10 は「自分が所有していない、あるいは許可を得ていないシステムに対して同種の試行を行ってはならない」 |

**特定の法解釈は断定していない。** 本文の該当箇所はすべて「〜という整理が一般的である」「〜という扱いが取られることが多い」「〜という運用にしている組織が多い」「制度によって異なる」という条件付きの記述にとどめ、GDPR の72時間や APPI の PPC 報告といった具体的な数値・手続は 28.14 の既存記述への参照で済ませ、新設節では再掲していない。

## 変更・新規作成したファイル

### 本文・参照

| ファイル | 種別 | 内容 |
|---|---|---|
| `03-part2-frontend.md` | 変更 | 6.11・7.9 新設、実装課題を 6.12・7.10 へ、課題6.6 追加 |
| `04-part3-backend.md` | 変更 | 13.25 新設、実装課題を 13.26 へ |
| `05-part4-data.md` | 変更 | 14.25・14.26・17.16 新設、実装課題を 14.27・17.17 へ、課題14.7・17.7 追加 |
| `07-part6-quality.md` | 変更 | 23.27・25.11 新設、実装課題を 23.28・25.12 へ、課題23.10 追加 |
| `08-part7-practice.md` | 変更 | 27.18・30.16 新設、実装課題を 27.19・30.17 へ、第VII部の総括の位置と本文を更新、30.14/30.15 からの参照を 30.17 へ更新 |
| `09-references.md` | 変更 | 一次資料11件を追加（後述）。既存の未解決引用 `[W3C WCAG, 2024]` を解消 |
| `01-toc.md` / `10-index.md` | 生成 | `generate:handbook` で再生成 |
| `LEARNING_LEVELS.md` / `LEARNING_PATHS.md` / `CODE_EXERCISES.md` / `CHAPTER_TEMPLATE.md` | 生成 | 各生成スクリプトで再生成 |
| `NARRATIVE_ARCHITECTURE.md` | 変更 | 第II部・第IV部・第VI部の要約と、第6・7・13・14・17・23・25・27・30章の因果の鎖を更新 |
| `README.md` | 変更 | 節数 405→415、演習 139→143、演習カード 143→147（追記のみ、既存記述は削除なし） |
| `CONTRIBUTING.md` | 変更 | クリーン環境区分の件数 139→143 |
| `CLEAN_ENVIRONMENT.md` | 変更 | ローカル自動 109→113、全演習件数 139→143 |
| `dist/` | 生成 | `build:site` で再生成 |

### 正本（config）

| ファイル | 内容 |
|---|---|
| `config/learning-levels.json` | 6.11、7.9、13.25、14.25、14.26、17.16、23.27、25.11、27.18、30.16 を追加。旧 6.11→6.12（450分）、7.9→7.10、13.25→13.26、14.25→14.27（510分）、17.16→17.17（730分）、23.27→23.28（600分）、25.11→25.12、27.18→27.19、30.16→30.17 へ改番。405節→415節 |
| `config/chapter-guides.json` | 第6・7・13・14・17・23・25・27・30章の到達目標、中核節、典型的な失敗、診断、判断、評価基準、一次資料を更新。改番も反映 |
| `config/learning-paths.json` | frontend・backend-db・infra-sre・security・tech-lead の該当ステージへ新設節を追加。**各ルートの章集合は変えていない**（`routeAppearances` を不変に保つため）。標準通読は必修のみの構成のため変更なし |
| `config/exercises.json` | 課題6.6・14.7・17.7・23.10 を schemaVersion 2 の全必須フィールド付きで追加（139→143演習） |
| `config/clean-environment-plan.json` | 4件を `local-automated` として追加。件数 139→143、local-automated 109→113 |
| `config/narrative-flow.json` | 第6章 11→12、第7章 9→10、第13章 25→26、第14章 25→27、第17章 16→17、第23章 27→28、第25章 11→12、第30章 16→17 へ `minimumBridgeCount` を更新（第12章・第27章の扱いは後述） |
| `narrative-flow.json`（リポジトリ直下の同内容コピー） | 上と同じ更新 |

### 検証スクリプト・照合対象

| ファイル | 変更 |
|---|---|
| `scripts/validate-clean-environment.mjs` | 演習正本とカテゴリ集計の件数 139 → 143 |
| `scripts/validate-clean-environment.test.mjs` | 件数 139 → 143、local-automated 109 → 113 |
| `scripts/apply-learning-levels.test.mjs` | 節数 405 → 415、全分類合計 205時間20分 → 219時間20分 |
| `scripts/validate-handbook.test.mjs` | 節数・学習メタデータ 405 → 415 |
| `beta-review-scope.json` | 第6・7・13・14・17・23・25・30章の `metrics.minimumBridgeCount` を正本と一致させた（詳細は後述） |

`package.json` は編集していない（新しいスクリプトを追加していない）。`LICENSE` / `LICENSE-TEXT` / `LICENSING.md` / `RELEASE_POLICY.md` / `CHANGELOG.md` / `ERRATA.md` / `config/release.json` / `.github/` / `scripts/build-site.mjs` / `scripts/validate-release-policy.mjs`（KEN-62 の成果物）も編集していない。`config/release.json` の判定規則への追記も不要だった（`validate:release-policy` が ERROR 0 / WARN 0 のまま通っている）。

### コード

| ファイル | 種別 | 行数 |
|---|---|---:|
| `code/ch06/a11y-focus/README.md` | 新規 | — |
| `code/ch06/a11y-focus/starter/main.ts` / `starter/report.ts` | 新規 | 406 / 4 |
| `code/ch06/a11y-focus/solution/main.ts` / `solution/report.ts` | 新規 | 463 / 4 |
| `code/ch14/data-lifecycle/README.md` | 新規 | — |
| `code/ch14/data-lifecycle/starter/main.ts` / `starter/report.ts` | 新規 | 407 / 4 |
| `code/ch14/data-lifecycle/solution/main.ts` / `solution/report.ts` | 新規 | 441 / 4 |
| `code/ch17/payment-integration/README.md` | 新規 | — |
| `code/ch17/payment-integration/starter/main.ts` / `starter/report.ts` | 新規 | 348 / 4 |
| `code/ch17/payment-integration/solution/main.ts` / `solution/report.ts` | 新規 | 400 / 4 |
| `code/ch23/abuse-defense/README.md` | 新規 | — |
| `code/ch23/abuse-defense/starter/main.ts` / `starter/report.ts` | 新規 | 284 / 4 |
| `code/ch23/abuse-defense/solution/main.ts` / `solution/report.ts` | 新規 | 345 / 4 |
| `code/ch06/solutions.test.ts` / `code/ch14/solutions.test.ts` / `code/ch17/solutions.test.ts` / `code/ch23/solutions.test.ts` | 変更 | テスト計19件追加 |
| `code/ch06/README.md` / `code/ch14/README.md` / `code/ch17/README.md` / `code/ch23/README.md` | 生成 | `generate:exercise-catalog` |

コード合計 3,126行（starter / solution / report）。

## 追加した一次資料

`09-references.md` へ、既存の形式と並び順に合わせて次を追加した。

**RFC・公式仕様**

- [ISO 4217] Codes for the representation of currencies（通貨コードと小数桁）
- [RFC 6585] Additional HTTP Status Codes（`429` と `Retry-After`）
- [W3C ARIA, 2023] Accessible Rich Internet Applications (WAI-ARIA) 1.2
- [W3C APG] ARIA Authoring Practices Guide
- [W3C WCAG, 2024] Web Content Accessibility Guidelines (WCAG) 2.2

**オンラインリソース・標準**

- [APPI/PPC] 個人情報保護委員会「個人情報の保護に関する法律についてのガイドライン」
- [Deque axe-core] axe-core rule descriptions
- [EU GDPR, 2016] Regulation (EU) 2016/679 の条文
- [OWASP ATP] OWASP Automated Threats to Web Applications
- [OWASP Credential Stuffing] Credential Stuffing Prevention Cheat Sheet
- [PCI DSS] Payment Card Industry Data Security Standard v4.0.1

**既存の未解決引用を1件解消した。** 6.9 の本文は「WCAG 2.2 [W3C WCAG, 2024]」と引用していたが、`09-references.md` には [W3C WCAG, 2018]（WCAG 2.1）しか無く、キーも版も一致していなかった。2.1 のエントリは残したまま 2.2 のエントリを追加して解決した。

過度な断定を避けるため、法令・標準のエントリにはすべて「適用の判断は法務へ確認する」「適用範囲と必要な対応は構成・取引量・契約によって異なる」といった注記を付けている。

## 実行した検証

```
pnpm run validate:exercises        → 終了コード0
pnpm run validate:narrative-flow   → 終了コード0
pnpm run validate:beta-review      → 終了コード0
pnpm run validate:release-policy   → 終了コード0
pnpm run validate:handbook         → 終了コード0
pnpm run check:handbook            → 終了コード0
```

`check:handbook` の内訳（すべて成功）:

| 検査 | 結果 |
|---|---|
| `apply:learning-levels:check` | 415節で差分なし |
| `generate:learning-paths:check` | 6ルートで差分なし |
| `apply:chapter-guides:check` | 30章で差分なし |
| `apply:exercise-rubrics:check` | 演習カード147件で差分なし |
| `generate:exercise-catalog:check` | 31ファイルで差分なし |
| `generate:handbook:check` | 目次・アンカー・索引で差分なし |
| `test:handbook` | 全25テスト成功、失敗0 |
| `validate:clean-environment` / `test:clean-environment` | 143演習、local-automated 113 / local-tls 7 / external-service 17 / browser-manual 6 |
| `validate:exercises` | 演習143件、観察課題4件、演習カード147 / 見出し147 |
| `validate:narrative-flow` | 30章すべて completed |
| `validate:beta-review` | 章30（core 15 / exercise-only 5 / sampled 10）、演習37（4530分）、必修節199 — **作業前と完全に同一** |
| `validate:release-policy` | ERROR 0 / WARN 0 |
| `validate:handbook` | **ERROR 0 / WARN 27** |

### ERROR / WARN 件数

- 作業前: **ERROR 0 / WARN 27**（すべて既存の `ANCHOR_DUPLICATE`）
- 作業後: **ERROR 0 / WARN 27**
- **増減なし。** 内訳も作業前と同一である（`#コード集の使い方` 18件、`#採用判断` 2件、`#仕組み` 1件、ADR の `#context` / `#decision` / `#consequences` / `#alternatives-considered` 各1件、計27件）。30.16 の `#### 使い方` は 30.14 と衝突するため、最初から `#### 使い方 ― 4領域チェックリスト` として作成し、警告を増やさないようにした。

### 章のコード教材の個別検証

```
pnpm --filter @handbook/ch06 typecheck / test   → エラー0 / 8件成功
pnpm --filter @handbook/ch14 typecheck / test   → エラー0 / 12件成功
pnpm --filter @handbook/ch17 typecheck / test   → エラー0 / 13件成功
pnpm --filter @handbook/ch23 typecheck / test   → エラー0 / 15件成功
node scripts/validate-exercises.mjs --chapter ch06 / ch14 / ch17 / ch23  → いずれも成功
tsx a11y-focus/solution/report.ts          → 4/4 → 0/4
tsx data-lifecycle/solution/report.ts      → 4/4 → 0/4
tsx payment-integration/solution/report.ts → 4/4 → 0/4
tsx abuse-defense/solution/report.ts       → 4/4 → 0/4
pnpm run build:site / build:site:check     → 再生成後に成功
```

starter 側も4件すべて型検査を通り、`report.ts` が例外なく実行できる（未実装のため 0/0 を出力する）ことを確認している。

`pnpm run validate:workspace` は本作業環境の Node.js が v26 のため作業前から失敗する（本書の固定版は 24.18.0）。この失敗は本変更とは無関係である。

## 完了条件の達成根拠

### 1. 実務頻度に応じた優先順位で既存章へ統合

- 4テーマの実務頻度を「遭遇する開発者の範囲 × 遭遇する頻度 × 既存記述の空白」の3点で評価し、a11y → 濫用対策 → 個人データ → 決済の順位を根拠つきで確定した（本レポート冒頭の表）。
- 優先順位を**節数（3/2/2/2）**、**配置の広さ**（a11y のみ第II部と第VI部へ分散）、**既存必修節との接続密度**（1位・2位は 6.9 / 13.1 / 23.6 の直系）へ反映した。
- 新規章は作らず、既存9章へ10節として統合した。章番号は振り直していない。実装課題節9件の番号だけを移動した。
- 各節は `NARRATIVE_EDITING_GUIDE.md` の方針どおり、前節で残った問題を受け取る接続文（`handbook:narrative-bridge`）から始まる。相互参照で 3.3 / 5.11 / 6.9 / 6.10 / 6.11 / 7.7 / 7.8 / 9.1 / 12.5 / 12.13 / 13.1 / 13.2 / 13.4 / 13.5 / 13.7 / 13.10 / 13.17 / 13.21 / 13.24 / 14.2 / 14.9 / 14.16 / 14.18 / 14.20 / 14.21 / 14.22 / 14.23 / 14.24 / 15.2 / 17.6 / 17.13 / 17.14 / 17.15 / 20.12 / 20.13 / 22.3 / 22.6 / 22.8 / 23.3 / 23.5 / 23.6 / 23.7 / 23.9 / 23.10 / 23.12 / 23.24 / 23.26 / 25.4 / 25.5 / 25.9 / 25.10 / 26.10 / 26.11 / 26.13 / 27.2 / 27.10 / 28.11 / 28.14 / 28.15 / 30.13 / 30.14 / 30.15 とつないでおり、既存記述の再説明はしていない。

### 2. セキュリティ・法務上の免責と確認先を明記

- 30.16 に「本書がここで扱えないこと ― 免責と確認先」を置き、**8つの判断について「なぜ本書で決められないか」と「確認先」を表にした**。あわせて一次資料の起点を別表で示した。
- 免責は 30.16 に集約せず、**14.25・14.26・17.16・25.11 の各冒頭と、判断が必要になる本文の該当箇所**にも置いた。該当節だけを読んだ読者にも届く。
- 「本書は法的助言ではない」「法域によって要件が異なる」「決済は PCI DSS の範囲を自分たちで結論づけず、カード情報を自前で扱わない」「個人情報保護法 / GDPR 等は必ず専門家に確認する」を、いずれも教材として使える形（何を確認先にするか、何を記録として残すかを含む形）で明記した。
- **特定の法解釈は断定していない。** 該当箇所はすべて条件付きの記述にとどめ、具体的な数値・手続は 28.14 の既存記述への参照で済ませている。
- 演習カード4件の「本番利用時の警告」にも、法務・専門家への確認と、本番データ・本番鍵・第三者システムへの適用禁止を明記した。

### 3. 最低1つの横断演習へ反映

- 4件（課題6.6・14.7・17.7・23.10）を追加した。4件とも「**失敗を実際に再現 → 対策 → 再現しなくなることを検証**」という KEN-50 の課題13.7 / 14.6、KEN-51 の課題12.6 / 17.5 / 17.6 / 23.9 と同じ構造を持つ。いずれも `runFindings` が同じ検査を2つの実装へ当て、`4/4 reproduced` → `0/4 remaining` を機械的に出力する。
- 4件とも `local-automated` で、**外部サービス・実ブラウザ・支援技術・決済事業者・第三者ホストへ一切接続しない**。判定は固定の `FIXTURES` と固定の基準時刻に基づき、現在時刻・プロセスのタイムゾーン・ネットワーク・乱数に依存しない。
- 各演習が 30.16 のチェックリストの区分と対応している（課題6.6 → A-2・A-3、課題14.7 → B-3、課題17.7 → C、課題23.10 → D-1・D-3）。この対応は 30.16 の末尾に明記した。
- `config/exercises.json` へ schemaVersion 2 の必須フィールドをすべて揃えて登録し、starter / solution は既存の複数ファイル型の命名規約（`exercise/starter/`、`exercise/solution/`、`exercise/README.md`）に従って作成した。章テスト計19件で再現と解消の両方を自動検証している。

## 積み残し・判断の記録

### `beta-review-scope.json` と `minimumBridgeCount` の扱い（重要）

`validate:beta-review` は `config/narrative-flow.json` の `minimumBridgeCount` を正本として、その全30章の**第75パーセンタイル**を選定基準 C4 のしきい値にする。**更新前に影響を全パターン計算した。**

作業前のしきい値は **14**。実際の接続文数は 第6章 11→12、第7章 9→10、第13章 25→26、第14章 25→27、第17章 16→17、第23章 27→28、第25章 11→12、第27章 18→19、第30章 16→17 に増えた（第12章は 16 のまま、KEN-51 の据え置き分）。

| 更新案 | しきい値 | 影響 |
|---|---:|---|
| 9章すべてを実数へ更新 | **14** | **第27章が C4 を獲得し、既存の C1 と合わせて2基準となり sampled → core へ昇格**。E2 により演習が必須検証演習に加わり、`BETA_REVIEW_SCENARIOS.md`（KEN-60 の成果物）、RL-EXEC のトラック・工数、RL-READ の記録対象章、RL-SPEC の章集合、`routeStageCoverage`、`totals` が連鎖して変わる |
| 第27章を 13 のまま据え置き、他8章を実数へ更新 | **14** | しきい値も C4 の該当章も不変。第6・7・25章は 12 以下のため C4 に届かず、第13・14・17・23・30章はもともと core のため tier は動かない。scope 側の更新は8章の `metrics.minimumBridgeCount` のみ |

**後者を採用した。** 理由は3つある。

1. `scripts/validate-narrative-flow.mjs` は `bridgeCount < minimumBridgeCount` をエラーとする。この値は**下限（回帰ガード）**であって実数の記録ではないため、13 のまま据え置いても検査は正しく機能し、基準を緩めることにもならない。
2. 第27章を core へ昇格させることは、ベータレビューの対象範囲を広げるという KEN-60 の設計判断の変更であり、KEN-52（本文の補完）の権限外である。
3. KEN-60 の成果物である `BETA_REVIEW_SCENARIOS.md` を書き換えずに済む。

なお、**第27章の宣言値 13 と実数の乖離は KEN-49 の時点で既に発生していた**（27.13〜27.17 の追加で実数 18、宣言値 13）。本作業はその据え置きを継承しつつ、実数を 19 へ増やしただけである。第12章についても KEN-51 の据え置き（宣言 13 / 実数 16）をそのまま維持した。

**`scripts/validate-beta-review.mjs` の基準は一切変更していない。** 結果として `validate:beta-review` の出力（章30、core 15 / exercise-only 5 / sampled 10、演習37・4530分、必修節199）は作業前と完全に一致している。

演習を4件追加しても E2 の選出は変わらない。第13章は E1 に難易度3の演習（13.6）を、第14章は 14.2 を、第17章は 17.1 を持つため E2 の対象外であり、第23章の E2 候補は id の数値順で先頭の 23.5 のままである（`23.5` < `23.10` の数値比較）。第6章は exercise-only、第7・25・27章は sampled のため E2 を持たない。新設演習はすべて `local-automated` のため `environmentDependent` も増えず、C3 と E1 に該当しない。**第6章は environmentDependent が1件のまま**であり（2件になると単独で core へ昇格する）、新設演習を `local-automated` にしたことでこれを保っている。

### 読了負荷

| 章 | 作業前（必修 / 全体） | 作業後（必修 / 全体） |
|---|---|---|
| 第6章 | 45分 / 6時間50分 | 45分 / 9時間45分 |
| 第7章 | 40分 / 5時間 | 40分 / 5時間25分 |
| 第13章 | 2時間5分 / 14時間20分 | 2時間5分 / 14時間45分 |
| 第14章 | 1時間40分 / 11時間20分 | 1時間40分 / 14時間40分 |
| 第17章 | 35分 / 12時間45分 | 35分 / 15時間40分 |
| 第23章 | 1時間20分 / 12時間5分 | 1時間20分 / 15時間 |
| 第25章 | 40分 / 5時間5分 | 40分 / 5時間30分 |
| 第27章 | 55分 / 9時間20分 | 55分 / 9時間45分 |
| 第30章 | 1時間5分 / 5時間 | 1時間5分 / 5時間15分 |
| 全分類合計 | 205時間20分 | 219時間20分（+14時間0分、+6.8%） |

必修のみの初回通読（199節・24時間5分）は変わっていない。WS4（KEN-32）の「新規内容が全体の読了負荷を過度に増やしていない」に照らし、初回通読の負荷は不変で、選択的に読む範囲だけが増えた形である。増加分14時間のうち、本文は240分（4時間）、残る10時間は演習4件（合計10時間）である。増加が大きい第6章（+2時間55分）、第14章（+3時間20分）、第17章（+2時間55分）、第23章（+2時間55分）は、いずれも内訳が「新設節25〜50分＋演習150分」であり、新設節は独立して読めるうえ、frontend・backend-db・infra-sre・security・tech-lead の各ルートのステージへ個別に配置してあるため、ルート単位では必要な節だけを選べる。

### 今後の候補

- **実ブラウザ・支援技術を使う発展課題**: 課題6.6 は文書モデル上の再現であり、実際のフォーカス挙動、`inert` の効果、読み上げの差は扱えない。`browser-manual` 区分の発展課題として追加する余地があるが、第6章の `environmentDependent` が2件になると単独で core へ昇格するため、KEN-60 側の収束後に判断するのが安全である。
- **実サービスを使う発展課題**: 決済事業者のテスト環境、実際の漏洩資格情報照合サービス、bot 判定サービスとの疎通は扱っていない。`external-service` 区分の候補だが、同じ理由で先送りした。
- **複合ウィジェットの実装**: 6.11 は「Tab はウィジェット間、矢印はウィジェット内」という判断の型までを扱い、コンボボックスやツリーの実装は [W3C APG] に委ねた。演習化の余地はある。
- **エクスポートのアーカイブ形式**: 14.26 は範囲と非同期化までを扱い、実際のアーカイブ生成（添付の同梱、大きな利用者での分割）は扱っていない。
- **決済の状態機械の網羅**: 17.16 は与信・確定・失敗・猶予・返金・申し立ての関係を図で示したが、事業者ごとの状態名の対応表は作っていない。事業者と版で変わるため、本書の版管理には載せにくい。
- **13.1 と 23.6 の記述の統合**: 13.25 で両者のねじれを解消したが、23.6 の実装例そのものは残してある。将来版で 23.6 のコードへ「13.25 も参照」の注記を足すかどうかは、必修節の分量とのトレードオフになる。

### 文体について

本文は既存章と同じ常体（だ・である調）で執筆した。新設10節・演習本文4件・演習カード4件・演習コードのコメントを機械的に走査し、**地の文に敬体の文末（です・ます・ません・でしょう・ください・ましょう・でした・ました）が0件**であることを確認している。検出された箇所はすべて `「メールアドレスを入力してください」` のような**引用符に囲まれたUI文言・エラーメッセージの例示**であり、既存の 6.9 と同じ扱いである。`code/chXX/*/README.md` と README・CONTRIBUTING・CLEAN_ENVIRONMENT の運用文書は既存の慣習どおり敬体で書いた。日本語の表記（濁点・半濁点・長音）も原文どおり保っている。

### 作業中に発生させ、修正した不具合の記録

節の挿入に使った Node.js の `String.prototype.replace` は、置換文字列中の `` $` `` を「一致箇所より前の文字列全体」へ展開する。27.18 の本文に `` `$` はドル以外にも使われ `` という記述があったため、`08-part7-practice.md` の第27章 27.1〜27.17 が丸ごと重複挿入された。挿入直後に `apply:chapter-guides` が `<a id="section-27-1"></a> が1件ではありません: 2` で失敗したことで検出し、重複ブロックを除去して復旧した。他の9節には該当する文字列が無く、影響は1ファイルに限られる。復旧後、第27〜30章の見出し構成、節数（415）、`generate:handbook:check` の差分なしを確認している。
