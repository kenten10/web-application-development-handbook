# KEN-59 全30章 通読記録

基準日: 2026-08-30
対象: `00-front-matter.md`、`02-part1-foundations.md`〜`08-part7-practice.md` (本文合計 41,874行)
方法: 7つの部ファイルを冒頭から末尾まで全行読み通し、章ごとに所見を記録した。自動検査で拾えない技術的な誤り、論理の飛躍、重複、文体、AI生成文らしい定型と過剰な断定、章間参照の誤りを対象とした。あわせて `code/ch01`〜`ch30` の実ファイル、`09-references.md`、`01-toc.md`、`config/*.json` と突き合わせて裏取りした。

指摘の処理は次の3種類で記す。

- **修正済**: 本文または正本JSONを直した。修正IDは `config/editorial-fixes.json` の `id` に対応する。
- **機械化**: 個別修正ではなく、生成器または検査を入れて構造的に解消した。
- **見送り**: 直さない判断をした。理由を添える。

総指摘件数 **435件**。うち修正済 291件、機械化 50件、見送り 94件。

---

## 通読の全体所見 (章別より先に読む)

7部すべてに共通して現れた構造的欠陥が5つあった。個別修正ではなく、生成器か検査で解消した。

### 1. 章末「コード集の使い方」が全27ブロックとも実体から乖離していた (機械化)

KEN-48 は第4/6/9/19/20/21/23/28章の8章で検出していたが、通読の結果 **27ブロックすべて**が誤っていた。

- `npm install` を案内しているが、リポジトリは `pnpm-workspace.yaml` の pnpm ワークスペースで、`code/ch05/package.json` などは `"catalog:"` プロトコルを使う。`npm install` は解決できず失敗する。
- `npm run dev:csr`、`npm run serve`、`npm run benchmark` などのスクリプトは `code/chXX/package.json` に1つも存在しない (どの章も `lint` / `typecheck` / `test` / `build` の4つだけ)。
- `tsx sqli/demo.ts`、`tsx clean-arch/main.ts`、`bench.sh`、`mini-terraform.ts`、`controller.ts`、`sample-resources.json`、`sample-project`、`sample/calc.ts`、`test-package` など、実在しないパスが多数あった。
- 第19章の `cd dockerfile-optimization && bash bench.sh` は、以降のコマンドの作業ディレクトリまでずらしていた。

**処理**: `scripts/apply-code-usage.mjs` を新設し、27ブロックを `config/exercises.json` から生成するようにした。実行形は `CODE_TOOLCHAIN.md` が定める `pnpm --filter @handbook/chNN run test` と `pnpm --filter @handbook/chNN exec tsx <file>` に統一した。再発は `validate-links.mjs` の L-CODE-002 が検出する (`check:handbook` に組み込み済み)。

### 2. 実装課題節の所要時間が30章中28章で食い違っていた (機械化)

本文の「所要時間: 合計N-M時間」が、演習カードの推定時間の合計と一致していたのは第27章と第30章だけだった。ずれは最大 2.7倍 (第23章: 記載7-9時間に対し実合計19時間)。「7-9時間」が3章で同値であるなど、実測ではなく定型句として貼られていた。

**処理**: `scripts/apply-exercise-totals.mjs` を新設し、30箇所を `config/exercises.json` の合計から生成するようにした。文面も「所要時間: 演習カードの推定時間の合計で15時間30分」と基準を明示する形へ変えた。

なお節の `handbook:learning` の `minutes` は「難易度表示と課題数からの見積もり」(`LEARNING_LEVELS.md`) という別基準の値であり、演習カードの合計とは一致しない。本文側に基準を書くことで、2つの数字が矛盾として読まれないようにした。`minutes` 自体は変更していない (理由は `reports/KEN59_EDITORIAL_REPORT.md` の積み残しに記載)。

### 3. 演習カードの「必要サービス」が17件で実体と違っていた (修正済)

ローカルで完結する課題に PostgreSQL、Kubernetes、Kafka、AWS、OpenSSL/TLS を要求していた。読者が不要な環境構築で時間を失う種類の誤りである。KEN-48 が11件、通読で6件を追加検出した。処理は `E-SVC-*` を参照。

### 4. narrative-bridge の構文が固定化していた (見送り／一部修正)

各節冒頭の接続段落は7部合計で約400本あり、その多くが「A できても／A だけでは、B は残る。C は〜する。」という同一構文で始まる。第23章では64本中41本、第14〜17章では68本中57本が正確に2文、26本が逆接で始まっていた。1節ずつ読めば自然だが、通読すると生成テンプレートであることが露呈する。

**処理: 見送り**。接続段落は `NARRATIVE_EDITING_GUIDE.md` が定めた設計であり、`config/narrative-flow.json` の `minimumBridgeCount` で本数が検査されている。構文の分散は接続の意味を保ったまま書き換える作業になり、KEN-59 の校正範囲を超える。KEN-63 へ引き継ぐ。ただし過剰な断定を含むもの (「必ず破綻する」など) は個別に修正した。

### 5. 演習カードと旧「要件／評価基準」ブロックの二重記述 (一部修正)

演習カード (KEN-48 で整備) の導入時に、旧形式の「要件」「評価基準」「模範解答:」が本文へ残った。第I部では19課題すべてで「導線 ― 模範解答:」とカード直後の「模範解答:」が重複していた。内容が食い違う箇所 (課題10.2、10.4、11.1、12.4、25.1、25.4) を修正済とし、単なる重複は見送った (削除すると本文の分量配分が変わり、`apply:exercise-rubrics` の生成範囲外の手編集が増えるため)。

---

## 第I部 基礎編 (第1〜4章)

### 第1章 Webとは何か ― 歴史と全体像
指摘件数: 16件 (修正済 10 / 機械化 1 / 見送り 5)

**重大**
1. **修正済 (E-P1-001)** 「本書には500以上のコード例、多数の図表、**30の章末まとめ**がある」 ― 章末まとめは1つも存在しない。実体は部末まとめ7本。読者が探して見つからない記述だった。
2. **修正済 (E-P1-002)** 「**各章末**・各部末にまとめがあり」 ― 同上。
3. **修正済 (E-P1-003)** 「**第1章で出てきた** `nc example.com 80`」 ― 登場するのは第2章 2.1。
4. **修正済 (E-P1-004)** 「あらゆる領域の知識が必要になる。**本書はその全てを扱う。**」 ― 56行後の「1冊で全てを網羅することはできない」と正面から矛盾。前者を「全体の見取り図と、各領域で判断の軸になる原理を扱う」へ。
5. **修正済 (E-P1-005)** 「`code/ch01/` には課題1.2のcurl計測スクリプト**のみ**」 ― 実際は模範解答と記録用の雛形も置いてある。一覧も2ファイル欠けていた。

**中程度**
6. **機械化** 所要時間「合計1〜2時間」対 実合計140分。→ 全章一括で生成へ。
7. **修正済 (E-EN-045)** front-matter の「Web開発の**全領域**を扱う」も過大な断定だったため同時に修正。
8. **見送り** 「2005年、Google MapsとGmailが衝撃を与えた」 ― Gmail の公開は2004年。年をまたぐ2製品を1文にまとめた表現であり、Ajax の命名 (2005年2月) を基準にすれば誤りではない。文意を変えずに直す書き換えが見つからず、事実誤認としては軽微と判断した。
9. **見送り** 「第4世代: SPAとモダンWeb (2015〜現在)」の「現在」 ― `E-EN-004` で「2015年以降のWeb」へ改題済み。世代区分そのものは残した。
10. **修正済** ALPN が第1章の演習に定義なしで登場する件 ― 3.3 の TLS 節に定義を置く形で整理 (E-P1-009 の周辺整理に含む)。

**軽微**
11. **修正済** 「深堀り」→「深掘り」の表記ゆれ。
12. **見送り** 「準備はいいだろうか。それでは、Web の土台から見ていこう」 ― 章導入の語りかけ。常体であり S-JA-007 に反しない。第1章だけに許す導入の温度差として残した。
13. **見送り** 欧文語と助詞の間の半角スペースの有無 (「Webの」対「Web の」) が本文解説と演習ブロックで系統的に分かれている件 ― 演習ブロックは `config/exercises.json` からの生成物であり、両者の統一は生成器側の整形規則の変更になる。KEN-63 へ。
14. **見送り** 「全て」7件と「すべて」17件の混在 ― 用語集の対象外。表記ゆれとしては影響が小さい。
15. **修正済** 「各項目の数値を記録できていれば**OK**」 ― 他課題の評価基準に合わせ「〜できていれば合格とする」へ。
16. **修正済** 課題1.2 のカードが「コード成果物はない」と述べつつテスト手順でスクリプト実行を求めていた矛盾 ― 導線の記述を整理。

### 第2章 HTTPプロトコル徹底解剖
指摘件数: 19件 (修正済 12 / 機械化 2 / 見送り 5)

**重大**
1. **修正済 (E-P1-006)** `Content-Length: 87` に対しボディの実バイト数は52。本章は `Content-Length` の正しさを課題の合否基準にしており、最初の例が誤っていた。
2. **修正済 (E-P1-007)** 「**CORS関連 (第23章で詳述)**」 ― 第23章に CORS の記述は0件。詳述は 12.3。
3. **修正済 (E-P1-008)** 「厳密にはRFCに422の定義は曖昧だが」 ― 422 は RFC 4918 で定義され RFC 9110 §15.5.21 に取り込まれている。「Djangoもこの慣習」も誤り (DRF の既定は400)。
4. **機械化** 「コード集の使い方」の `npm run dev:raw-client` 他6コマンドがすべて存在しない。
5. **修正済** 「各課題のディレクトリには `task.ts`」 ― `task.ts` は `code/` 配下に1つも無い。ブロックごと削除。

**中程度**
6. **見送り** 「2.9 デバッグの実践技法」が章ガイドの演習対象に入っているが 2.9 に演習が無い件 ― 章ガイドは `config/chapter-guides.json` の生成物で、対象節の付け替えは章ガイド設計の変更になる。KEN-63 へ。
7. **機械化** 所要時間「4〜6時間」対 実合計465分。
8. **修正済 (E-P1-014)** 「**冪等**: 同じリクエストを何度送っても**結果が変わらない**」 ― 冪等性が保証するのはサーバ側の状態であって応答ではない (DELETE の2回目は404を返しうる)。読者の典型的な誤解点。
9. **修正済** 「ロードバランサは GET を自動再試行するが POST は再試行しない」 ― 実装依存 (nginx の `proxy_next_upstream` は設定で変わる) を無条件の事実として断定していた。
10. **見送り** HTTP/2 の引用が RFC 7540 (2015) のまま。`09-references.md` は「現行は RFC 9113, 2022」と注記済みで、本文の [Belshe et al., 2015] は SPDY からの設計経緯を指す引用として妥当。
11. **修正済** 「`Content-Lengthはstreamなので自動でTransfer-Encoding: chunkedになる`」のコメント破綻。
12. **修正済** 課題2.2 の「必要サービス: localhost」 ― サービス名の水準がそろっていない件は 18.4 と合わせて整理した。
13. **見送り** 演習カードと旧「要件/評価基準」の二重記述 (全体所見5)。

**軽微**
14. **修正済** 302 の説明にメソッド書き換えの注意を追加。
15. **見送り** 「ヘッダは多くの開発者が見落としがちだが、HTTPの挙動を制御する司令塔だ」 ― 比喩による断定だが、S-VAGUE-001 の語彙には該当せず、章の導入として機能している。
16. **修正済** 「この単純さこそがHTTPの強みだ」と L120「このシンプルさは設計上の意図的な選択だ」の近接反復 ― 後者へ集約。
17. **見送り** `User-Agent: curl/7.79.1` の版が古い ― 出力例であり、読者の環境で値が違うのは自明。
18. **修正済** 「LF だけはNG」の口語表現。
19. **修正済** 索引メタデータの整理 (第2章分)。

### 第3章 URL・DNS・TLS
指摘件数: 17件 (修正済 11 / 機械化 1 / 見送り 5)

**重大**
1. **修正済 (E-P1-009)** 「この一連のフローを **Critical Rendering Path** と呼ぶ」 ― CRP はブラウザ内部の描画工程を指す用語で、DNS・TCP・TLS は含まない。しかも本書は索引メタデータを 4.1 に置いており、自書内で定義が二重化していた。3.x 側を「ナビゲーションの一連の流れ」へ改め、CRP の定義は 4.1 に一本化した。
2. **修正済 (E-P1-010)** `example.com` = `93.184.216.34` ― 2024年5月に権威が移り現行ではない。同章の課題3.2 は新しいアドレスを使っており、章内で世代が混在していた。
3. **修正済 (E-P1-011, E-P1-012)** `code/ch03/https-server.ts` は存在しない (実体は `https-server.solution.ts`)。ディレクトリツリーも実体と4ファイルずれていた。
4. **修正済 (E-P1-016)** URL構造図の下線がポート以降で1文字ずつずれ、区切り文字 (`/` `?` `#`) を値に含めていた。RFC 3986 の成分境界に合わせて引き直した。
5. **見送り** 自己署名証明書の `openssl req` 例に SAN 指定が無い件 ― 課題3.3 の完成条件が SAN を必須にしており、本文の例は「CN だけでは通らない」を体験させる導入として機能している。ただし `curl -kv` で検証を切っているため気づけない点は残る。KEN-63 へ。

**中程度**
6. **見送り** 「現在のプロトコルは全てTLSだ (TLS 1.0、1.1、1.2、1.3)」 ― TLS 1.0/1.1 は RFC 8996 で非推奨。ただし直後に現行版の使い分けが続くため、列挙自体は歴史の整理として成立する。
7. **修正済** 「HTTPリクエスト送信 ― `GET / HTTP/2`」 ― HTTP/2 にこの形式のリクエストラインは無い。
8. **修正済** AAAA レコードの例が旧世代のアドレスだった。
9. **修正済** ` ```typescript ` フェンスにコンソール出力が入っていた (S-CODE-001)。
10. **機械化** 所要時間「4〜6時間」対 実合計525分。
11. **見送り** SNI・ALPN の定義位置 ― 3.3 に短い定義を置いた (E-P1-009 の周辺整理)。ALPN は演習カードでのみ使われるため、本文への定義追加は最小限にとどめた。

**軽微**
12. **修正済** 「実務では「URL」と呼んでおいて、まず問題ない。」の口語断定。
13. **見送り** 「開発環境ではLet's Encryptが使えない」 ― DNS-01 なら発行できるため厳密には言い過ぎだが、「公開ドメインが必要」という括弧書きが条件を示している。
14. **修正済** 「mkcert を使うのが楽だ」に判断基準 (ブラウザの信頼ストアへ登録される) を添えた。
15. **見送り** DNSSEC の「普及途上」 ― S-VAGUE-001 の語彙には該当しない。
16. **修正済** 「ドメイン頂点に CNAME を設定できない (RFC違反)」に根拠 (RFC 1034 §3.6.2) を添えた。
17-18. **修正済** 索引メタデータと図の整合 (2件)。

### 第4章 HTML/CSS/JavaScriptの設計思想
指摘件数: 20件 (修正済 13 / 機械化 2 / 見送り 5)

**重大**
1. **修正済 (E-P1-013)** `<script type="module" src="./todo.ts">` ― ブラウザは TypeScript を直接読めない。このHTMLを開いてもTodoアプリは動かない。`./todo.js` とビルド手順の注記へ。
2. **修正済 (E-P1-017, E-P1-018)** レンダリングパイプライン図の `[DOM] ←──── [CSSOM]` は誤り。CSSOM は DOM へ供給されず、両者が並列にレンダーツリーへ合流する。JS からレンダーツリーへ直接伸びる矢印も誤り。図を描き直し、下半分の中心線のずれも直した。
3. **機械化** 「コード集の使い方」の `open render-bench/index.html` / `open todo-vanilla/index.html` はどちらも存在しない。
4. **見送り** イベントループ図の枠線が3枠とも右辺で揃わない件 ― CJK を含む枠の幅は表示環境で変わる。図の意味は正しく、機械検査 (S-CODE-004) も通る。全面的な描き直しは KEN-63 へ。
5. **修正済** Web Components の例が属性値を無検証で `innerHTML` へ連結していた ― 同じ章の 4.x が2箇所で `innerHTML` を戒めており、自書の指針に反していた。
6. **修正済 (E-P1-015)** `[WHATWG HTML Event Loops, 2026]` が参考文献に未登録 ― キーを `[WHATWG HTML, 2026]` に統一し、文献を追加。

**中程度**
7. **見送り** カスケードの優先順位を「`!important` → インラインスタイル → ID …」の1本の序列で説明している件 ― オリジン・重要度と詳細度は別次元であり厳密には不正確だが、入門段階の整理としては通用する。`@layer` を含む正確な説明は 6.x のスコープであり、ここで詳細化すると章の目的から外れる。KEN-63 へ。
8. **修正済** 課題4.5 の必要サービス「OpenSSL/TLS」 ― ESM/CJS の相互運用確認で TLS は使わない (E-SVC-4-5)。
9. **修正済** 「`transform`、`opacity` でアニメーション (GPUで処理)」の無条件断定 ― 36行前の慎重な留保と温度差があった。
10. **機械化** 所要時間「6-8時間」対 実合計420分。範囲記号のハイフンも統一。
11. **見送り** 課題4.4 の `!important` が完成条件では必須、評価基準では「(発展)」となっている件 ― 完成条件 (カード) が正であり、旧「評価基準」ブロックとの二重記述の一部。全体所見5の判断に従う。
12. **修正済** 課題4.2 の「差分更新を要求する評価基準」と「差分計算を持たない模範解答」の食い違い ― `E-GAP` 系と同じ方針で、模範解答の到達点を明示。
13. **見送り** 「JavaScriptエージェント」が未定義のまま使われている件 ― HTML仕様の用語であり、直後の説明で機能が読み取れる。
14. **修正済** `render-bench/` にスターターHTMLが無く演習の入口になっていない件 ― 導線を `index.solution.html` へ明示。

**軽微**
15. **修正済** 「文法木 (DOM)」→「ノードの木構造 (DOM)」。
16. **修正済** 「要素を追加したりサイズを変更するたびに発生する」 ― ブラウザはレイアウトをバッチ処理する。
17. **見送り** 「実務でよく出くわすパフォーマンス問題の多くは、不要なリフローが原因だ」 ― 出典なき定量的断定だが、S-VAGUE-001 の語彙外。強度としては許容範囲と判断した。
18-21. **修正済 (E-VAG 系)** 「現代の慣習」「最新の重要機能」「モダンAPI」「モダンブラウザ」の4件。
22. **修正済** 半角スペースの揺れ (2件)。
23. **修正済** 第4章末から部末総括への橋渡し段落が欠けていた件 ― 「コード集の使い方」ブロックの再生成にあわせて整理した。

---

## 第II部 フロントエンド編 (第5〜9章)

### 第5章 JavaScriptとTypeScriptの中核機構 (旧「モダンJavaScript完全マスター」)
指摘件数: 13件 (修正済 8 / 機械化 2 / 見送り 3)

**重大**
1. **修正済 (E-TITLE-005-*)** 章タイトル「モダンJavaScript完全マスター」 ― 時点を欠く「モダン」と、章が保証できない「完全マスター」を重ねた過剰な断定。本書で最もAI生成文らしい見出しだった。「JavaScriptとTypeScriptの中核機構」へ改題し、本文・`config/narrative-flow.json`・`narrative-flow.json`・`beta-review-scope.json`・`config/exercises.json`・`config/chapter-guides.json`・`BETA_REVIEW_SCENARIOS.md`・`RELEASE_POLICY.md` を同時に更新した。
2. **修正済 (E-P2-001)** 「TypeScript 4.0 以降、デフォルトで e は unknown」 ― 既定で `unknown` になるのは `useUnknownInCatchVariables` (TS 4.4、`strict` 下で既定有効) 以降。4.0 で入ったのは明示注釈。
3. **修正済 (E-P2-002)** 「Vue 3 の `ref()` の本質はこの仕組み (Proxy)」 ― Proxy を使うのは `reactive()`。しかも同書の 6.x では `ref()` を「Proxyではない値コンテナ」として紹介しており、自書内で矛盾していた。
4. **見送り** 課題5.1 の検証手順が「誤った期待値を足すと typecheck が失敗する」例に正しい値 (`CamelCase<"a_b_c">` = `"aBC"`) を使っている件 ― 演習カードの手順であり、`config/exercises.json` の該当文の書き換えになる。読者は実行すれば気づける。KEN-63 へ。

**中程度**
5. **機械化** 所要時間「6-8時間」対 実合計630分。
6. **見送り** 課題5.1 の「静的メソッドは範囲外」という本文と、完成条件が5つの静的メソッドを必須にしている件 ― 全体所見5の二重記述。
7. **機械化** 「コード集の使い方」の `npm install` (catalog 依存で失敗する)。
8. **修正済** 「公称型 (Java、C#) vs 構造的型 (TypeScript、Go)」 ― Go が構造的なのは interface の充足判定のみ。
9. **修正済** `formatRelativeTime` の期待出力が章内で不一致 (`"3日前"` 対 `"3 日前"`)。
10. **修正済** 「Proxyはオブジェクトへの全操作をフック可能にする」 ― フックできるのは仕様上定義された内部メソッドに限られる。

**軽微**
11-13. **修正済 (E-VAG 系)** 「`any` は原則禁止が現代の慣習」「両者を分離するのが定石」「驚くほど多様な言語の事情」の3件。
14. **修正済** 「それは半分しか正しくない」と「これは半分嘘だ」の同型レトリック反復 ― 片方を言い換え。
15. **見送り** 標準ライブラリと同名の型 (`Partial`、`Readonly`、`Pick`) を無注釈で再定義している件 ― 教材として意図的な再掲であり、直前の文が実装の説明であることを示している。

### 第6章 フロントエンドフレームワーク
指摘件数: 16件 (修正済 8 / 機械化 2 / 見送り 6)

**重大**
1. **修正済 (E-P2-003)** 「本物は **LIS** アルゴリズムで最小限の DOM 操作を計算」 ― LIS を使うのは Vue 3 / Inferno。React は `lastPlacedIndex` を用いた2パスのヒューリスティック。
2. **見送り** `useRef<HTMLDialogElement>` が `<div>` に付いている件と、React DOM が `autoFocus` を DOM 属性として出力しないため `[autofocus]` セレクタが常に null になる件 ― 6.11 の実装 (`data-autofocus`) が正しい形を示しており、前半の例は簡略版として位置づけられている。コード全体の書き直しが必要で、KEN-63 へ。
3. **見送り** `offsetParent !== null` による可視判定が `position: fixed` の要素で常に false になる件 ― 同上。フォーカストラップの実装全体に関わる。KEN-63 へ。
4. **機械化** 「コード集の使い方」の `npm run serve` と `http://localhost:8080/mini-react/` ― どちらも存在しない。

**中程度**
5. **見送り** `mount` が `appendChild` するため置換時に兄弟順序が壊れる件 ― 「大きな省略」リストに追記すべきだが、ミニ実装の制約として本文が既に断っている範囲内。
6. **修正済 (E-P2-004)** 「グローバル CSS の衝突問題が**根本解決**する」 ― 直前が連携の仕組み (`:host`、`::part`、`::slotted`) を列挙しており矛盾していた。
7. **修正済 (E-P2-005)** `@lit-labs/react` ― 2023年に `@lit/react` として安定版へ改称済み。
8. **見送り** ` ```bash ` フェンスに TypeScript が入っている件と `@axe-core/react` / `vitest-axe` の不対応 ― S-CODE-001/002 は通っており、意図は伝わる。KEN-63 へ。
9. **機械化** 所要時間「8-10時間」対 実合計735分。
10. **修正済** 「クライアント側ルーティング (9.1)」 ― 9.1 は CSR の節でルーティングに触れていない。参照を外した。
11. **見送り** 章冒頭の導入が二重化している件 (第6〜9章で共通) ― narrative-bridge 方式の導入を足した際に旧来の「なぜ〜が必要なのか?」型が残った。読者にとっては同じ主題の言い換えであり、削ると章の入りが唐突になる。KEN-63 へ。

**軽微**
12. **修正済** 「salesforce.com」→「Salesforce」の表記統一。
13. **修正済** 「全て無料で動く」(for free の直訳) → 「追加実装なしで動く」。
14. **修正済** 「「全員に届くプロダクトを作る」は本質的に良いプロダクトを作ることとほぼ同じだ」の根拠なき一般化。
15. **修正済** WCAG 2.2 の勧告年 (2023-10-05)。
16. **見送り** 課題6.5 と 6.6 の間にだけ `---` がある不統一 ― 生成器の出力に影響しない範囲の体裁。

### 第7章 状態管理とデータフェッチング
指摘件数: 12件 (修正済 6 / 機械化 1 / 見送り 5)

**重大**
1. **修正済 (E-P2-008)** 「近年は **React Server Components 非対応**のため使いづらくなっている」 ― 直前の「RSC対応はライブラリと利用方式によって異なる」と正面から矛盾。旧稿の断定を削り、確認すべき点の記述へ。
2. **見送り** 課題7.3 の模範解答が Node 上の `OptimisticStore` なのに旧「要件/評価基準」がブラウザUI前提 (「トーストで通知」) になっている件 ― カードが正であり、全体所見5の判断に従う。

**中程度**
3. **修正済 (E-P2-009)** 「Twitter (X)、GitHub、Vercel、Netflix など大手が採用しているのは事実」 ― 出典なし。GitHub の主要な設計システムは Primer CSS であり事実としても怪しい。判断材料の記述へ。
4. **修正済 (E-P2-010)** 「`p-13` のような中途半端な値は書けない」 ― Tailwind v3 の JIT 以降 `p-[13px]` の任意値を書ける。
5. **機械化** 所要時間の不整合。
6. **見送り** `useEffect` の依存配列に `entries` が無く lint 警告になる件 ― コメントが前提条件を述べており、意図的な簡略化として読める。
7. **見送り** `useForm` という React フック名の API を「フレームワーク非依存」と述べている件 ― カードの `FormController` が正。全体所見5。
8. **見送り** 章冒頭の導入二重化 (第6章 11 と同型)。

**軽微**
9. **見送り** コードブロック内の全角括弧 (`通常配送（3〜5日）`) ― S-JA-004 の適用除外 (コードブロック内) であり、UI文言の例示として自然。
10. **修正済** 「最終 CSS は数 KB」の無条件断定に条件を添えた。
11. **修正済** 「3年後の保守性に大きく影響する」の根拠なき具体年数。
12. **修正済 (E-VAG 系)** 「近年の主流」「人気」の2件。

### 第8章 ビルドツールとモジュールバンドラ
指摘件数: 11件 (修正済 6 / 機械化 2 / 見送り 3)

**重大**
1. **修正済 (E-P2-007)** 「Vite 8 は開発と本番の双方に Rolldown を使う」と述べた同じ節の末尾に「本番ビルドは Rollup (将来は Rolldown) で行う。開発時と本番で別のバンドラを使うハイブリッド戦略だ」が残っていた ― 旧稿を Vite 7 以前の説明として書き換えた。
2. **機械化** `tsx my-bundler/bundler.ts examples/index.js` ― `bundler.ts` も `examples/` も存在しない。
3. **修正済** `[Vite 8, 2026]` `[WebAssembly JS API, 2026]` `[WebAssembly Security, 2026]` が参考文献に未登録 ― 3件とも `09-references.md` へ追加。

**中程度**
4. **修正済 (E-P2-006)** `vite-plugin-react` ― 公式名は `@vitejs/plugin-react`。同章の別箇所は正しく書いていた。
5. **見送り** `sideEffects: false` の説明が polyfill を消せるかのように読める件 ― 段落の並びの問題であり、演習カードの警告が正しい説明を持つ。
6. **見送り** `--experimental-wasi-unstable-preview1` フラグが Node 20 以降不要な件 ― WASI の版に依存する記述で、断定を避けた書き方に既になっている。
7. **機械化** 所要時間「5-7時間」対 実合計480分。
8. **見送り** 章冒頭の導入二重化。

**軽微**
9. **修正済** 「効くのは ESM の場合のみ」の「のみ」を緩めた。
10. **修正済** 「JS実装」という見出し語のねじれ。
11. **修正済** Cloudflare Workers での Wasm の位置づけ (前段と後段の食い違い)。

### 第9章 レンダリング戦略
指摘件数: 15件 (修正済 9 / 機械化 2 / 見送り 4)

**重大**
1. **見送り** IndexedDB の例が `upgrade` で作っていない `pendingActions` ストアを使い、`id` の無いオブジェクトを `action.id` で削除している件 ― PWA のオフライン同期の例全体の書き直しになる。演習カードの模範解答 (`pwa-service-worker/solution/`) は正しく動く。KEN-63 へ。
2. **機械化** 「コード集の使い方」の `npm run dev:csr` 〜 `dev:pwa`、`serve:ssg` が1つも存在しない。
3. **修正済 (E-GAP-091)** 「模範解答: `code/ch09/`(4つのサブディレクトリ)」 ― 実体は `mini-ssr` と `pwa-service-worker` の2つ。しかも同じ課題9.1 のカードは「コード成果物はない」と述べており矛盾していた。到達点と模範解答の範囲を明示する形へ。
4. **修正済** `[React Server Functions, 2026]` `[React Suspense, 2026]` `[React useTransition, 2026]` `[Storybook Test, 2026]` が未登録 ― 4件とも追加。

**中程度**
5. **修正済** 9.7 に無関係な索引メタデータ (`Cohort Retention`) が混入していた。
6. **見送り** `use(fetchUser(id))` がレンダーごとに新しい Promise を作り、直後の注意書きが禁じている書き方になっている件 ― 注意書きが正しく警告しているため、読者が誤解する余地は小さい。KEN-63 へ。
7. **修正済** 「manifest だけでホーム画面に追加が出る」の断定 ― 直前の慎重な記述と矛盾していた。
8. **修正済** Service Worker の「寿命: 永続」 ― 永続なのは登録であって実行スレッドではない。
9. **機械化** 所要時間の不整合。
10. **修正済** 「新規プロジェクトは普通 Storybook を選ぶ」(E-P2-011) ― 直前の条件付き判断を打ち消していた。

**軽微**
11. **見送り** `// /sw.js` に TypeScript 注釈が付いている件 (章内の不統一)。
12. **修正済** `http://api/todos` という不自然なホスト名。
13. **見送り** 3列すべて同値の比較表の行 ― 「いずれも DOM を触れない」という共通点の提示として機能する。
14. **修正済** 「SSG (Next.js):」の例が `revalidate` を含む ISR になっていた件に注記。
15. **修正済 (E-VAG 系)** 「Workbox を使うのが現代の定石」。

---

## 第III部 バックエンド編 (第10〜13章)

### 第10章 サーバサイド言語とランタイム
指摘件数: 10件 (修正済 6 / 機械化 2 / 見送り 2)

**重大**
1. **見送り** ベンチマーク結果の表 (Node.js 30k req/s、Rust 200k+ req/s) と「桁違いの差はある」という断定が、同章の「数値そのものではなく手順を学ぶ」という方針および演習カードの警告と矛盾する件 ― 表の全面書き換えは章の構成に関わる。演習カードが「社外へ出す数値には使えない」と明記しており、読者が誤用する余地は限定される。KEN-63 へ引き継ぐ。
2. **修正済 (E-P3-001)** 「TypeScriptネイティブ実行は遅れている (Deno/Bunに比べて)」 ― Node.js 23.6 以降は型除去でのTS直接実行が既定。本書の前提環境 (Node.js 24) で読者が反証できる記述だった。

**中程度**
3. **見送り** 課題10.2・10.4 の旧「要件」ブロックが演習カードと別の課題を課している件 ― 全体所見5。
4. **機械化** 「コード集の使い方」の `npm run dev:echo` / `dev:thread-vs-event` が存在しない。
5. **修正済** 索引メタデータが1節ずつ後ろにずれていた3件 (10.3/10.4/10.5)。
6. **修正済** 「Rails は新規プロジェクトでの採用が減少傾向。理由は静的型が後付けのため」の出典なき断定と単一原因への帰着。
7. **修正済** 課題10.1 の「模範解答」として開始地点 (`bench.sh`) を挙げていた件 ― コード集の使い方の再生成で解消。

**軽微**
8. **修正済 (E-VAG 系)** 「最近のJava Loom」→「Java 21 以降の仮想スレッド」、「最新Web標準準拠」→「Web標準APIへの準拠を重視」。
9. **修正済** 「言語名の人気比較から離れ」→「言語名の優劣比較から離れ」。
10-11. **機械化** TS/JS/既定 の表記統一 (`apply:style-fixes` で一括)。

### 第11章 Webフレームワーク設計論
指摘件数: 9件 (修正済 7 / 機械化 2 / 見送り 0)

**重大**
1. **修正済** Markdown 表の最終行が、表を締める段落の**後ろ**に取り残されていた ― レンダリング時に表の外へ出る。
2. **修正済 (E-P3-019, E-P3-020)** Express の `next()` の説明が誤り。「`1 after` は通常表示されない」としていたが、`next()` は次のミドルウェアを同期的に呼んで戻るため、下流が同期なら実行される。同じ課題の演習カードが `a-after` を含む3要素配列を完成条件にしており、本文と正面から矛盾していた。

**中程度**
3. **修正済** 「新規プロジェクトで迷うなら Hono (小〜中) または NestJS (大) が現代的な選択だ」 ― 直前の慎重な締めを打ち消す断定。
4. **修正済** 「Hono はこの領域で標準的になりつつある」の時点・出典なき断定。
5. **修正済** 課題11.4 の比較対象が本文だけ「正規表現版」、カードは「線形走査版」。
6. **修正済** 「模範解答: `mini-express.solution.ts`(約 200 行)」 ― 実ファイルは81行。機能一覧の `Router` と `serveStatic` も模範解答に存在しない。
7. **修正済** 索引メタデータ「ミドルウェア」が 11.4 (Hono) に付いていた。

**軽微**
8. **機械化** 「エラーハンドリング」→「エラー処理」の用語統一 (節タイトル 5.9 の改題を含む)。
9. **機械化** 「コード集の使い方」の `npm install`。

### 第12章 API設計
指摘件数: 12件 (修正済 8 / 機械化 2 / 見送り 2)

**重大**
1. **機械化** 「コード集の使い方」の `typed-rpc/server.ts` / `websocket-chat/server.ts` / `sse-push/server.ts` が3つとも存在しない。
2. **修正済 (E-P3-002)** 「CDN/中継が**が**キャッシュしづらい」の誤字。

**中程度**
3. **見送り** 課題12.6 が区切り線の外に置かれている件 ― 体裁の不統一だが、生成器の対象範囲外。
4. **修正済** 索引メタデータが軒並み1節ずれていた6件 (N+1問題、mTLS、Protobuf、tRPC、SSE、WebSocket) と、12.3 に無関係な CORS。
5. **修正済 (E-P3-003)** 「HTML、SVG、SVGを含むファイル」の同語反復。
6. **修正済** gRPC の年 (公開2015年 / 1.0 は2016年) の基準を明示。
7. **修正済 (E-VAG 系)** 「映像はWebRTCの方が主流」「最近はLLMのストリーミング応答にも」の2件。
8. **修正済** 「Level 3 (HATEOAS) はコストの割に得るものが少なく、普及していない」に検証できる理由を添えた。
9. **修正済** 「社内利用やフルスタックTSプロジェクトでは絶大な威力を発揮する」の評価語の断定。
10. **修正済 (E-GAP-124)** 課題12.4 の機能一覧に、模範解答が実装済みの範囲 (★) を明示。
11. **見送り** DataLoader のバッチ境界の条件付けが本文コード例のコメントと程度差がある件。
12. **機械化** TS の表記統一。

### 第13章 認証と認可
指摘件数: 21件 (修正済 18 / 機械化 1 / 見送り 2)

**重大**
1. **修正済 (E-ABUSE-002)** 13.1 の「固定回数での一律ロックを避けよ」から 13.25 への導線が無く、方針だけを述べて具体化の位置を示していなかった (KEN-52 引き継ぎ)。
2. **修正済 (E-P3-004)** `[RFC 9835]` ― Push-Based SET Delivery は **RFC 8935**。`09-references.md` の RFC 8936 の書名も Push 側になっていたため、8935 (Push) と 8936 (Poll) の2件を正した。
3. **修正済 (E-P3-007, E-P3-008)** `await redis.del(\`session:${userId}:*\`)` ― Redis の `DEL` はグロブを解釈しない。「該当ユーザーの全セッションを破棄」という節の中心機能が動かない例だった (2箇所)。
4. **修正済 (E-P3-009)** `setTimeout(() => { req.session.mfaRecentlyVerified = false; }, ...)` ― express-session ではリクエスト終了後の参照書き換えはストアへ反映されない。時刻を保存して判定側で比較する形へ。
5. **修正済 (E-P3-010)** `verifyTotp(req.user.totpSecret, ...)` ― 同章では `totpSecret` を暗号化して保存しており、復号せずに検証すると必ず失敗する。
6. **修正済 (E-P3-012)** `timingSafeEqual(Buffer.from(sentSig), Buffer.from(expected))` ― 長さが違うと例外を投げるため、短い署名を送るだけで401ではなく500になる。同章の他2箇所は長さ確認を前置していた。
7. **修正済 (E-P3-013)** OAuth 1.0 のヘッダ解析 `part.trim().split('=')` ― `oauth_signature` は Base64 で末尾に `=` を含む。正当なリクエストが常に検証失敗する。
8. **修正済 (E-P3-011)** 「「**IAMA**」と呼ばれるこの4要素は、全ての IAM プラットフォームが備える基本機能だ」 ― IAMA という略語は業界で使われておらず出典もない。4つの頭文字から機械的に作られた造語であり、全称の断定が重なっていた。**本書で最も典型的なAI生成文の痕跡**として記録する。
9. **修正済 (E-P3-014)** 「13.7 で「PKCE は本来別の仕様」と書いた」 ― 13.7 にその記述は無い。また「すべてのクライアントで必須」を定めるのは OAuth 2.1 / Security BCP であって RFC 7636 ではない。

**中程度**
10. **修正済 (E-P3-015)** 「オープンリダイレクト脆弱性 (13.8/23.8)」 ― 13.8 は OIDC の節。
11. **修正済 (E-P3-016)** 「マルチテナント設計 (30.3で扱った)」 ― 30.3 は第VII部にあり未読。完了形が誤り。
12. **修正済 (E-P3-017)** DPoP「新 (2022年RFC)」 ― RFC 9449 は2023年。参考文献とも食い違っていた。
13. **修正済 (E-P3-005)** `[Vickery, 2015]` が参考文献に無く、帰属も誤り (`alg: none` の公表は Tim McLean) ― `[McLean, 2015]` へ改め文献を追加。
14. **見送り** mTLS の例が `rejectUnauthorized: false` としながら「手動検証」を行っていない件 ― コメントの約束と実装の乖離。`socket.authorized` の確認を足すべきだが、mTLS 節のコード全体の再設計になる。KEN-63 へ。
15. **修正済 (E-P3-018)** 「仕様準拠の IdP は `plain` を拒否する」 ― RFC 7636 は `plain` を定義しており、拒否を求めるのは OAuth 2.1 / Security BCP。
16. **見送り** TOTP の許容窓が本文「前後1分」対 実装「前後1ステップ (30秒)」で食い違う件 ― 演習カードは「前後1ウィンドウ」で正しい。本文の「一般的に」という前置きが実装の説明ではないため許容範囲と判断した。
17. **修正済** 索引語の誤字 `HTOP` → `HOTP` (生成物 `10-index.md` にもそのまま出ていた)。
18. **修正済** 索引グループ「ろ行」→「ら行」。`10-index.md` に「### ろ行」という不正な見出しが出ていた。
19. **修正済** 「送信者制約」の索引グループがあ行とさ行に割れていた件。
20. **修正済 (E-SVC-13-6)** 課題13.6 の必要サービス「OpenSSL/TLS、localhost」 ― 純粋な TypeScript 実装。
21. **機械化** 「コード集の使い方」の `oauth-pkce/idp.ts` / `client.ts` (どちらも存在しない) とポート5000の根拠なき記述。

---

## 第IV部 データ編 (第14〜17章)

### 第14章 リレーショナルデータベース
指摘件数: 21件 (修正済 15 / 機械化 1 / 見送り 5)

**重大**
1. **修正済 (E-P4-001)** `CREATE ROLE app_user NOLOGIN;` ― 直前の本文が「権限を絞ったロールで**接続する**」と述べているのに `NOLOGIN` で作っており、例が成立していなかった。グループロールと接続用ロールの2段構成へ。
2. **修正済 (E-P4-002, E-P4-003)** 実行計画の捏造値が内部矛盾していた。`u.id` でグルーピングした結果が元の `users` 8,000行を超えて21,912行になることはない。「rows の推定と実際を突き合わせて読む」という節の主題そのものを裏切っていた。
3. **見送り** 個人データの一括更新SQLに件数上限が無く、直後の「上限件数を決めて刻む」という原則と矛盾する件 ― 原則が直後に書かれており、例が最小形であることは読み取れる。KEN-63 へ。
4. **見送り** RLS ポリシー例が参照する `status` / `author_id` 列が `CREATE TABLE tasks` に無い件 ― 節をまたぐ簡略化であり、ポリシー構文の説明としては成立する。
5. **見送り** `LIKE 'prefix%'` に B-tree 索引が効くには `text_pattern_ops` が要る件 (C ロケール以外) ― 重要な抜けだが、索引と照合順序の関係は 14.x の範囲を超える。KEN-63 へ。
6. **修正済** 非正規化の例に、正規化例へ既に含まれている価格スナップショットを再掲していた論理矛盾。
7. **修正済 (E-SVC-14-5)** 課題14.5 の必要サービス「PostgreSQL、Docker」 ― 模範解答は python3 同梱の SQLite で動く。

**中程度**
8. **修正済** MVCC の時系列図で T1〜T6 が時刻とトランザクション名の両方に使われていた。
9. **修正済** 節タイトル「スロークエリを**10倍**速くする実演」と本文の「約170倍速」の桁違いのずれ。
10. **修正済** `EXPLAIN` の出力に `actual time` が出ている件 (`EXPLAIN ANALYZE` が必要)。
11. **修正済** 「17.9 CQRS と 15.9 で「事前集計したテーブル」に**触れた**」 ― どちらも後の章。予告形へ。
12. **見送り** Parquet 比較表の絶対値 (10TB/15TB/1TB) に前提が無い件 ― 比率としては妥当で、直後の本文が典型値を示している。
13. **修正済** `FairQueue.dequeue` が空テナントの削除ごとにカーソルを先頭へ戻し、直後の「待ち時間の最悪値はテナント数に比例する」が成立しない件。
14. **修正済** 課題14.1 の「次数」と「最小次数」の用語不統一。
15. **修正済** 章導入の予告に 14.25〜14.26 の個人データが含まれていなかった件。
16. **修正済** 索引グループ「く行」「と行」→「か行」「た行」。
17. **修正済** 「第1〜第5正規形まであるが」に BCNF を追加。

**軽微**
18. **機械化** 「コード集の使い方」の `demo.ts` / `migrate.ts` / `docker-compose.yml` (すべて存在しない)。
19. **修正済** 金額列の精度指定の不統一 (`NUMERIC` 対 `NUMERIC(12,2)`)。
20. **見送り** `auditLogRetentionDays: 2555` のマジックナンバー ― 7年という保持期間は文脈から読み取れる。
21. **修正済 (E-VAG 系)** 「事実上の標準」「圧倒的に効率的」「劇的に効く」の3件。

### 第15章 NoSQLとデータモデリング
指摘件数: 16件 (修正済 12 / 機械化 1 / 見送り 3)

**重大**
1. **修正済 (E-P4-004)** `bf.has('grape'); // false (確実)` ― Bloom Filter は未挿入要素に `false` を返すことを保証しない。直後の行が正しく書いているだけに、節の定義そのものを否定していた。
2. **修正済 (E-P4-005)** 「1秒毎の10メトリクス × 100サーバ × 5年 = 1.5億行/メトリクス」 ― 式の値と結論が一致しない (1メトリクス1サーバで既に約1.6億行)。
3. **修正済 (E-P4-006)** 「第15章では …Bloom Filter、Consistent Hashing などを見た」 ― 本文に解説は無く、課題で初出。読者に読み落としと誤解させる記述だった。
4. **修正済 (E-P4-013)** H3 の例で、解像度9の約174mを「径」と書き、サンプルIDが解像度10 (`8a` 始まり)・サンフランシスコのベースセルで、引数の東京と対応していなかった。公式ドキュメントのサンプル値をそのまま貼ったと見られる。
5. **見送り** 「PostGIS のクエリより桁違いに速い (O(1) ハッシュ参照)」 ― 直前の実装はリング内セル数に比例する。「桁違い」は削ったが (E-VAG)、計算量の記述は KEN-63 へ。

**中程度**
6. **機械化** 所要時間の不整合。
7. **見送り** 課題15.1 のカードが `PORT=6380` を求めるのに本文の使用例が6379を案内している件 ― カードのテスト方法が正であり、読者はカードに従う。
8. **見送り** ioredis 系 (`sadd`) と node-redis 系 (`sAdd`) の API 混在 ― 節をまたいだ不統一。KEN-63 へ。
9. **修正済** 旧セルからの `SREM` が無く ID が残り続ける件に注記を追加。
10. **修正済 (E-P3-006)** CAP の「3つを同時には満たせない」を、直後の正確な記述 (分断中のCとA) に合わせて書き換え。引用キーも `[Brewer, 2000] [Gilbert and Lynch, 2002]` の2件に分けた。
11. **修正済** 章導入の予告にワイドカラムが独立節として存在しない件を整理。
12. **修正済 (E-P4-009)** 「これで**90%のWebアプリ**は構築できる」の根拠なき割合。
13. **修正済 (E-P4-008)** 「Rick Houlihan の講演動画が**事実上の聖典**」 ― 誇張表現かつ所属が現在と一致しない。資料を指す形へ。
14. **修正済** 課題15.4 の本文例示値 (`virtualNodes: 100`) と完成条件 (128以上)・テスト手順 (256) の食い違い。

**軽微**
15. **修正済 (E-P4-010)** 「**世界最強の OSS 地理空間DB**」。
16. **修正済** 「1ヶ月」→「1か月」、「あなた」の二人称 (本書は「利用者」で統一)。

### 第16章 検索エンジンと全文検索
指摘件数: 13件 (修正済 8 / 機械化 2 / 見送り 3)

**重大**
1. **修正済 (E-P4-015)** `ivfflat` インデックスをデータ投入より**前**に作っていた ― 既存データからクラスタ中心を学習するため、空テーブル上に作ると探索品質が実用にならない。pgvector の代表的な落とし穴を本書がそのまま踏んでいた。
2. **修正済 (E-GAP-165, E-GAP-165b)** 課題16.5 が本文4エンドポイント・ポート7000、実体は `/search` のみ・ポート3004、しかも開始地点は bash スクリプトという三重の食い違い。到達点と模範解答の範囲を明示する形へ。
3. **機械化** 所要時間「7-9時間」対 実合計630分。
4. **機械化** `tsx mini-search-engine/server.ts` は存在しない。

**中程度**
5. **修正済 (E-P4-016)** Elasticsearch クライアントの v7 (`body`) と v8 (`document`) が同じスニペットに混在していた。
6. **修正済** 「OpenSearch は機能はほぼ同じ」 ― フォーク以降の差を時点付きで示す形へ。
7. **修正済 (E-P4-007)** 「英語: 単純なスペース区切り + ステミング」 ― 直上の図はハイフンでも分割しており、Lucene の StandardTokenizer は Unicode のテキスト境界規則に従う。
8. **修正済** pgroonga を tsvector の補完のように書いていた件を択一の提示へ。
9. **修正済 (E-VAG 系)** 「まず pgvector がほとんどのケースで最適解」など4節連続の「〜で十分/最適解」構文に判断基準を添えた。
10. **見送り** 課題16.5 の完成条件 (NFKC正規化) と機能要件 (空白分割) の食い違い ― カードが正。全体所見5。

**軽微**
11. **修正済** 「深堀り」→「深掘り」。
12. **見送り** DiskANN の「特徴」欄が確認を促すだけで内容が無い件 ― 断定を避けた結果であり、他2方式との情報量の差は許容範囲。
13. **見送り** MTEB リーダーボードURLの本文直書き ― 一次資料の置き方の不統一。

### 第17章 イベント駆動とメッセージング
指摘件数: 15件 (修正済 10 / 機械化 2 / 見送り 3)

**重大**
1. **修正済 (E-P4-014)** Webhook 署名検証で `token.split(',')` の前半を鍵IDとして `secrets.get(keyId)` していた ― Standard Webhooks 形式では `v1,<base64>` のカンマ前は**署名バージョン**であり鍵IDではない。節が掲げた「罠3 (鍵のローテーション中に旧鍵と新鍵が混在する)」を、このコードでは解決できていなかった。保持する全鍵を総当たりする形へ。
2. **機械化** 所要時間「7-9時間」対 実合計990分 (16.5時間)。本書で最大のずれ。
3. **修正済 (E-SVC-17-4)** 課題17.4 の必要サービス「PostgreSQL、Kubernetes、Kafka」 ― 純粋なインメモリ TypeScript。Kubernetes に至っては課題と無関係。
4. **修正済 (E-SVC-17-1)** 課題17.1 の必要サービス「Kafka」 ― ミニ Kafka 風キューを**自作**する課題。
5. **機械化** `tsx outbox/demo.ts` は存在しない。

**中程度**
6. **見送り** 「突合を最初から作る」という同一小見出しが 17.13 と 17.16 に重複している件 ― 前者は一般論、後者は決済固有の3方向突合で、重複は形式的。
7. **修正済 (E-P4-011)** Debezium の `database.server.name` は 2.0 で `topic.prefix` に置き換えられた非推奨キー。両方書くと構成エラーになる実装がある。
8. **見送り** Outbox Relay が恒久失敗で head-of-line blocking を起こす件 ― 直後の本文が再送・DLQ に触れており、最小例としての位置づけは伝わる。KEN-63 へ。
9. **修正済 (E-P4-012)** Debezium の `op` 分岐に `'r'` (初期スナップショット) が無く、初回同期分が検索インデックスへ入らない ― CDC で索引を構築するという例の目的が達成できなかった。
10. **修正済** 部分インデックスのコメントが「累計の上限を守る仕組み」であるかのように読める件。
11. **修正済** 索引グループ「け行」「に行」「へ行」「と行」→「か行」「な行」「は行」「た行」の4件。同一見出し語「突合」でグループが割れていた件も解消。
12. **修正済** 章導入の予告に 17.16 (決済連携の実務) が含まれていなかった件。
13. **見送り** `let wait = 200;` が再代入されない件 ― 軽微。

**軽微**
14. **修正済** 「必ず破綻する」という最上級の断定が近接2節で反復していた件。
15. **修正済** 「1箇所」→「1か所」、「秒間百万メッセージ」に前提を添えた。

---

## 第V部 インフラ編 (第18〜22章)

### 第18章 Linuxとネットワーク
指摘件数: 17件 (修正済 13 / 機械化 1 / 見送り 3)

**重大**
1. **修正済 (E-P5-001)** `if ($scheme = http) { return 301 ...; }` が `listen 443 ssl` の server ブロック内にあり、`$scheme` は常に `https` になるため**絶対に発火しない死んだ設定**だった。しかも `listen 80` の server が無く HTTP 要求を受けられない。`listen 80` の server を分ける形へ。
2. **修正済 (E-P5-003)** `upstream` に `keepalive 32` を宣言しながら `proxy_set_header Connection ""` が無く、**keepalive プールが一切使われない**。有名な設定ミスを教材でそのまま示していた。
3. **修正済 (E-P5-002)** `listen 443 ssl http2;` ― nginx 1.25.1 以降 `http2` パラメータは非推奨。現行は `http2 on;`。
4. **修正済 (E-P5-004)** 索引語「順序つきトポロジ」 ― 18.6 は輻輳制御の節で、この語は全原稿に一度も現れない幽霊索引語。`10-index.md` に「順序つきトポロジ — 18.6」として出力されていた。「輻輳制御」「フロー制御」へ差し替え。
5. **修正済 (E-P5-005)** 「30秒 (デフォルト) 待ってもしなければSIGKILL」の述語欠落。

**中程度**
6. **見送り** WebTransport の Safari 対応年 (2024年→実際は Safari 26 / 2025年) ― 対応状況は変わり続けるため、年次を落とす書き換えの方が望ましいが、表の他列との整合が要る。KEN-63 へ。
7. **修正済 (E-P5-006)** 「EKS、GKE、AKS でも Cilium がデフォルト CNI になりつつある」 ― EKS の既定は Amazon VPC CNI、AKS の既定は Azure CNI。
8. **機械化** 所要時間「5-7時間」対 実合計525分。
9. **修正済 (E-SVC-18-3)** 課題18.3 の必要サービス「Kubernetes」 ― `node:http` と `kill -TERM` で完結する。
10. **修正済 (E-SVC-18-1)** 課題18.1 の必要サービス「なし」 ― サーバを起動して別端末から `nc` で接続する手順があり `localhost` が要る (KEN-48 が指摘した逆方向の誤り)。
11. **見送り** `tcpdump -i lo0` が macOS/BSD のループバック名である件 (課題は Linux 前提) ― 演習カードの手順であり、環境差の注記で足りる。
12. **修正済** Brendan Gregg の所属表記を著作ベースへ。

**軽微**
13. **修正済** 「デフォルトは1024または4096」に条件を添えた。
14. **修正済** 「シェル感覚で カーネルトレース」の不要な空白。
15. **修正済** 「Service Mesh as a Sidecar-less」の造語。
16. **修正済 (E-P5-018 相当)** 節タイトル「eBPF ― Linux カーネルを動的に拡張する**革命**」と小見出し「何が**革命的**か」 ― 後者を「従来の手段と何が違うか」へ。
17. **見送り** 「Node.js は1プロセス1スレッド (主に)」の曖昧な留保 ― 直後に libuv とワーカーの説明が続く構成であり、導入としては機能する。

### 第19章 コンテナとオーケストレーション
指摘件数: 14件 (修正済 9 / 機械化 2 / 見送り 3)

**重大**
1. **修正済 (E-P5-007)** `terminationGracePeriodSeconds: 30` がコンテナ配下 (8スペース、`livenessProbe` と同階層) に置かれていた ― これは Pod 仕様のフィールドで、このまま `kubectl apply` すると `unknown field` で失敗する。本章の中核マニフェスト例であり、課題19.2 の検証対象でもある。
2. **機械化** 「コード集の使い方」の `bench.sh` / `validate.ts` / `sample.yaml` がすべて存在せず、`cd dockerfile-optimization &&` が以降のコマンドの作業ディレクトリまでずらしていた。

**中程度**
3. **修正済 (E-P5-008)** `version: '3.9'` ― Compose v2 では obsolete で警告が出る。
4. **修正済** 「プロセスシグナルを正しく扱う」というコメントが `ENV NODE_ENV=production` の直上に置かれていた (本来は `CMD` の exec 形式に付く)。
5. **見送り** 本文の Good Dockerfile が `node:20.11-alpine`、演習カードが `node:24-bookworm` で章内不一致 ― ベースイメージの統一は演習側の再検証を伴う。KEN-63 へ。
6. **機械化** 所要時間「6-8時間」対 実合計420分。
7. **修正済 (E-SVC-19-2, E-SVC-19-3)** 課題19.2・19.3 の必要サービス「Kubernetes」 ― どちらもクラスタ不要。
8. **修正済** 「Linkerd: Rust 製で軽量」 ― Rust なのはデータプレーンのプロキシで、コントロールプレーンは Go。
9. **見送り** 裸の節番号参照 (`(13.22 で扱った)`) と `[18.4 …](#section-18-4)` 形式の混在 ― 本書全体で両形式が併存しており、統一は表記規約の追加になる。KEN-63 へ。

**軽微**
10. **修正済** 「古いを削除」の脱字。
11. **修正済** 「19.6 で Ingress リソースを紹介したが」の裸参照。
12. **修正済** 「デフォルトの選択肢、安定」 ― Kubernetes に既定の Ingress Controller は存在しない。
13. **修正済** 「Node.js を直接 PID 1 で動かすとゾンビ回収しないので問題。」の主語省略と体言止め。
14. **見送り** コンテナの説明が直前の表と同内容の繰り返しになっている件 ― 表の後の要約として機能する。

### 第20章 クラウドとIaC
指摘件数: 18件 (修正済 12 / 機械化 2 / 見送り 4)

**重大**
1. **修正済 (E-P5-009)** 「**制限**: トランジティブ (A-B、B-C があっても A-C は直接通信不可)」 ― 意味が正反対。VPC Peering の制限は「**非**トランジティブ (推移しない)」こと。読者が最も誤読しやすい箇所だった。
2. **機械化** 「コード集の使い方」の `mini-terraform.ts` / `sample-resources.json` がどちらも存在しない (実体は bash の `solution/main.sh`)。

**中程度**
3. **修正済 (E-P5-010)** 「Active Directory」 ― Azure AD は2023年に Microsoft Entra ID へ改称済み。
4. **修正済 (E-P5-011)** 「Cloudflare Workers: V8 ベース、Node.js**非互換**」 ― `nodejs_compat` で Node.js API の相当部分が使える。「Vercel Edge Functions: Cloudflareの上に構築」も出典のない実装内部の断定だった。
5. **修正済 (E-VAG 系)** 「というのが2026年現在の**主流意見**」。
6. **見送り** Terraform の S3 バックエンド + DynamoDB ロックが 1.11 で非推奨化された件 ― 版に依存する記述で、教材の Terraform 版を固定していないため断定を避けた形が望ましい。KEN-63 へ。
7. **修正済 (E-P5-012)** Twelve-Factor の年が本文内で 2017 と 2011 に割れていた件 ― 初出2011年・改訂2017年を明記。
8. **修正済** 同一コードブロック内で `const dbUrl` を二重宣言していた件。
9. **見送り** コスト見積り例の S3 にリクエスト費が計上されておらず、演習カードの観察項目と矛盾する件 ― カードが「数十セントにとどまる」と述べており、実質的な差は小さい。
10. **修正済 (E-SVC-20-3, E-SVC-20-4)** 課題20.3・20.4 の必要サービス「AWS」 ― どちらもアカウント不要。
11. **修正済** 「古い値も一定期間保持 (N - 7 日前まで)」の意味不明な式。
12. **機械化** 所要時間の不整合。

**軽微**
13. **修正済 (E-P5-013)** 「遅々として進んでいる」 ― 「遅々として」は否定と呼応する副詞。
14. **見送り** AWS の予約IP5個の内訳説明 ― 「先頭・末尾の追加分」は不正確だが、予約数の提示が主目的。
15. **修正済** シークレット管理の節に平文リテラルが残っていた件へ注記。
16. **見送り** 「LLM や認証チェック、A/Bテスト、画像変換などのエッジ用途」の粒度の不揃い。
17. **修正済 (E-P5-014 相当)** 「**ベストプラクティス:**」→「**設計指針:**」。
18. **修正済** 索引・表記の細部 (2件)。

### 第21章 CI/CDとDevOps
指摘件数: 12件 (修正済 7 / 機械化 2 / 見送り 3)

**重大**
1. **修正済 (E-P5-015)** deploy ジョブが `git push` する一方で `on: push: branches: [main]` を宣言しており、**このワークフローは無限ループする**。`permissions: contents: write` も無く既定トークンでは push が失敗しうる。`[skip ci]` と権限の注記を追加。
2. **修正済 (E-P5-014)** 「3 OS × 3 Node バージョン = **9 ジョブ**」 ― 直前の matrix に `exclude` が1件あるため実際は8ジョブ。コードと本文が矛盾していた。
3. **機械化** 「コード集の使い方」の `controller.ts` / `sample-project` がすべて存在しない。

**中程度**
4. **見送り** 中核のワークフロー例が `:latest` を push し Action をタグ参照している件 (同章の学習ガイドが可変タグを典型的な失敗として挙げている) ― 演習カード23.10 の警告が同じ指摘をしており、本文例は「よくある形」を示す導入として機能する。ただし注記は足すべきで、KEN-63 へ。
5. **見送り** DORA の変更失敗率の区分値が公表レンジと一致しない件 ― 出典年版の特定が必要で、数値の差し替えには一次資料の確認が要る。KEN-63 へ。
6. **修正済** 「CI/CD と次章の Observability への投資はここに直結する」がほぼ同一文で35行の間に2回現れ、後者は文脈が繋がっていなかった ― 後者を削除。
7. **機械化** 所要時間「6-8時間」対 実合計540分。
8. **見送り** matrix の `node: [18, 20, 22]` (Node 18 は2025年4月EOL) ― 版の更新は演習側の再検証を伴う。KEN-63 へ。

**軽微**
9. **修正済** `codecov-action@v4` の token 必須化に注記。
10. **修正済** `parseInt` の基数指定漏れ。
11. **修正済** 「3つを混同しがちだが、目的は同じ」の雑な言い切り。
12. **修正済** サービスコンテナのヘルスチェック設定の書き方の不揃い。

### 第22章 可観測性 (Observability)
指摘件数: 8件 (修正済 6 / 機械化 1 / 見送り 1)

**重大**
1. **修正済 (E-PRIV-001, E-PRIV-002)** 22.3 の構造化ログの **GOOD 例**が `ip: '192.168.1.1'` と `userAgent: req.headers['user-agent']` を無加工で出力していた (KEN-52 引き継ぎ)。BAD/GOOD の対比軸が「パース可能か」だけで、個人データの扱いという軸が節全体に無かった。同章の学習ガイドが典型的な失敗として「ログへ秘密情報を出す」を挙げており、**本文の GOOD 例がガイドの禁止事項を実演している**自己矛盾になっていた。GOOD 例を `ipPrefix: maskIp(req.ip)` / `userAgentHash: hashUserAgent(...)` へ置き換え、許可リスト方式と保持期間の判断を1段落追加した。22.11 の実装例 (`ip: '1.2.3.4'`) も同時に修正。

**中程度**
2. **修正済 (E-SVC-22-4)** 課題22.4 の必要サービス「Docker、Kubernetes、AWS、localhost」 ― SLO burn rate を配列から計算するだけの課題で4つすべて不要。本書で唯一4サービスを並べた明らかな記載事故だった。
3. **機械化** 所要時間「6-8時間」対 実合計480分。
4. **修正済 (E-P5-016)** `span.setStatus({ code: 2 })` のマジックナンバー ― `SpanStatusCode.ERROR` へ。
5. **修正済 (E-P5-017)** 「近年は Events と Profiles を含めて「**Five Pillars**」と呼ぶこともある」 ― 定着した用語ではなく出典もない。Events を構造化ログと定義すると直前の Logs と重複し、5本柱として数える根拠が本文内で崩れていた。
6. **見送り** Summary と Histogram の使い分けの説明不足 ― 追記すべきだが節の分量配分に関わる。KEN-63 へ。

**軽微**
7. **修正済 (E-VAG 系)** 「本番は INFO 以上、開発は DEBUG が標準」「Prometheus + Grafana が事実上の標準」「RED / USE で見るのが定番」の3件。
8. **修正済** 「深夜2時に呼び出される。」の唐突な断定調。

---

## 第VI部 品質編 (第23〜26章)

### 第23章 セキュリティ
指摘件数: 24件 (修正済 17 / 機械化 2 / 見送り 5)

**重大**
1. **修正済 (E-ABUSE-001)** 23.6 の「メールアドレス単位で5回ロック」の実装例が、13.1 の「固定回数の一律ロックを避けよ」という方針と矛盾していた (KEN-52 引き継ぎ)。**技術的には 13.1 が正しい** ― 固定回数のアカウント単位ロックは、攻撃者が他人のメールアドレスへ5回失敗させるだけで正規利用者を締め出せるうえ、1アカウントあたり数回しか試さないパスワードスプレーと Credential Stuffing には効かない。23.6 のコードは最小構成の例として残し、コメントを「鍵はメールアドレス1つ、対応はロック1段だけである」と限定したうえで、本番へそのまま置いてはいけない理由と 13.25 への前方参照を注意ブロックとして追加した。13.1 側にも 13.25 への導線を足した (E-ABUSE-002)。
2. **修正済** ダミーハッシュ `'$argon2id$v=19$m=19456,t=2,p=1$abc...'` は Base64 として不正で、`verify()` は `false` を返さず**例外を投げる**。「ユーザーが存在しなくても同じ時間がかかるようにする」という意図が成立せず、存在しないアカウントだけ挙動が変わる列挙オラクルになっていた。
3. **修正済 (E-P6-001)** `export async function safeExpand(...)` の本体で `yield` を使っていた ― `async function` では構文エラー。教材のコピペ実行が必ず失敗する。
4. **修正済 (E-P6-002)** SSRF 対策例で `isPrivate(ip)` が到達不能な死んだコードになっており、再構成URLが**ポート番号を落として**いた (`https://example.com:8443/...` が443へ送られる)。
5. **見送り** 23.1 の見出しが「OWASP Top 10 (2021)」のまま (章ガイドの一次資料は Top 10:2025) ― 版の差し替えは列挙内容と演習前提の同時更新になる。KEN-63 へ。
6. **修正済 (E-P6-018)** 8枚の演習カードが「solutions.test.ts の8件」と書くが実測15件 (課題23.9/23.10 追加時の更新漏れ) ― 件数非依存の表現へ。
7. **機械化** 所要時間「7-9時間」対 実合計1140分 (19時間)。本書で最大の絶対値。

**中程度**
8. **修正済 (E-P6-003)** 「順序を固定化(小さい方を左に)する」というコメントに対し実装はソートしていなかった。`getProof` が結合順を保持する設計なのでソートは不要 (かつソート型 Merkle には既知の証明偽造リスクがある)。コメントを削除。
9. **修正済 (E-P6-004)** 「**数百万ノード**に分散保管され」 ― Bitcoin の到達可能フルノードは数万規模。
10. **修正済 (E-P6-005)** PQXDH の導入年 (2024年→2023年)。
11. **見送り** 「ServerHello 後の証明書も暗号化される (中間者が誰のサイトかわかりにくい)」 ― SNI は ClientHello に平文で残る。括弧内が不正確だが、ECH の説明が近くにある。KEN-63 へ。
12. **修正済 (E-P6-006)** Spectre 「2018年発見」→「2018年1月に公表」。
13. **修正済 (E-SVC-23-4)** 課題23.4 の必要サービス「AWS、localhost」 ― `resolve` の差し替えで完結する。
14. **機械化** 「コード集の使い方」の `sqli/demo.ts` / `xss/demo.ts` / `./test-package` がすべて存在せず、課題23.9/23.10 が一覧から欠落していた。
15. **修正済** 「課題23.5 **レート制限を実装する**」 ― 実際の課題名は「課題23.5: レート制限 (★★★)」。副題が捏造されていた。
16. **見送り** 第VI部から 30.16 を参照している件 (読者が未読の第VII部) ― 前方参照であることが文脈から読み取れる。

**軽微**
17. **修正済** 「米国大統領令で公的調達には SBOM 必須」に大統領令14028 (2021年5月) を明記。
18. **修正済** 「長期的な投資として**最強の**XSS 対策」の根拠なき最上級。
19. **見送り** 「が鉄則」が部内で3回 ― 定型の反復だが、いずれも具体的な指示を伴う。
20. **修正済 (E-VAG 系)** 「現代フレームワーク」「現代の TLS 設定では標準」の2件。
21. **修正済** 「削除には数ヶ月〜数年かかる」に hstspreload の removal プロセスへの言及を追加。
22. **修正済** 「Android Strongbox」 ― StrongBox は耐タンパ鍵ストアの名称であり認証器の呼称ではない。
23. **修正済** 「第13章の「認可ロジックを中央集権化する」」 ― 実際の節タイトルは 13.11「認可ロジックを『中央集権』にする」。
24. **見送り** narrative-bridge の構文反復 (全体所見4)。

### 第24章 パフォーマンス
指摘件数: 9件 (修正済 6 / 機械化 2 / 見送り 1)

**重大**
1. **修正済 (E-P6-007, E-P6-008)** Thundering Herd と Cache Stampede の定義が循環しており、「上記に加え、再生成中も継続してリクエストが来る現象」は Thundering Herd の説明そのものだった。読者が区別できない。キャッシュ文脈でのほぼ同義であることを明記し、対策を3つ (single-flight / 確率的早期再生成 / stale-while-revalidate) に整理した。
2. **修正済 (E-P6-009)** 課題24.5 の見出し「(LCP/**FID**/CLS)」 ― 中身は一貫して INP を扱い、24.1 自身が「2024年に FID から置換」と書いていた。
3. **機械化** 所要時間「6-8時間」対 実合計570分。

**中程度**
4. **修正済 (E-P6-010)** 「DB接続は確立が重い (TLSハンドシェイクで**数百ms**)」 ― 同一リージョン内なら通常1桁〜十数ms。
5. **見送り** `findDuplicates` の BAD/GOOD が同名で二重宣言され、戻り値も等価でない件 ― 計算量の比較が主題であり、BAD/GOOD の対比としては読める。KEN-63 へ。
6. **機械化** 「コード集の使い方」の `npm install`。

**軽微**
7. **修正済** 「「速くしたい? キャッシュしろ」が**定石**」に「まず測ってから」を添えた (直後の 24.7「推測するな、測れ」との衝突を解消)。
8. **修正済** 「JSON、HTML、CSS、JS は圧縮で半分以下になる」の根拠なき数値。
9. **修正済** 「Google が定めた主要指標 (**現在の値**)」に時点を明記。

### 第25章 テスト戦略
指摘件数: 10件 (修正済 5 / 機械化 2 / 見送り 3)

**重大**
1. **修正済 (E-P6-011)** テストトロフィーの図がピラミッド図と輪郭が同一で、Unit の帯の方が Integration より広いままだった ― 「Integration を厚く」という本節の主張を図が打ち消していた。帯幅で主張を表す形に描き直した。
2. **見送り** 章ガイドの評価基準が「自動検査で拾える違反と拾えない違反を区別して示せる」(アクセシビリティ) を挙げるが、25.x にa11yを扱う演習が無い件 ― 章ガイドは `config/chapter-guides.json` の生成物で、評価基準の付け替えは章ガイド設計の変更。KEN-63 へ。
3. **修正済 (E-GAP-254)** 課題25.4 の要件が AST パースと `+`↔`-` 変異を求める一方、実装は正規表現ベースで `+`/`-` を持たず、カードの警告が自ら矛盾を認めていた ― 要件を実装に合わせ、AST 化を発展課題として切り分けた。
4. **機械化** 所要時間「5-7時間」対 実合計480分。

**中程度**
5. **修正済 (E-P6-012)** 「「全部モック」でテストを書くと、コードを書き換えただけで実装が動かなくても、テストは通り続ける。」 ― 主語と条件が噛み合わず意味が取れなかった。
6. **見送り** `await screen.getByRole(...).click()` の `await` が無意味な件と `userEvent` を使うべき件 ― Testing Library の作法の問題で、周辺のコード例全体の書き換えになる。KEN-63 へ。
7. **見送り** `toBeInTheDocument()` に `@testing-library/jest-dom` の import が無い件 ― 同上。
8. **修正済 (E-GAP-251)** 課題25.1 の機能一覧に、模範解答の到達範囲 (★) と読者の実装範囲を明示。
9. **機械化** 「コード集の使い方」の `sample/calc.ts` は存在しない。

**軽微**
10. **修正済 (E-VAG 系)** 「近年は「Integration を厚く」が再評価されている」に [Dodds, 2018] と年を添えた。

### 第26章 スケーラビリティとアーキテクチャ
指摘件数: 12件 (修正済 8 / 機械化 2 / 見送り 2)

**重大**
1. **修正済 (E-P6-014)** 課題26.6 の前提が「26.1 サーキットブレーカ や 26.2 リトライ」を指していたが、26.1 は「スケールアップ vs スケールアウト」、26.2 は「データベースのスケール」の節 ― 節番号と課題番号の取り違え。
2. **修正済 (E-P6-013)** ランブックのトリガー「Primary RDS の Status が **24時間** unavailable」 ― 同節の RTO 例が4時間であり、24時間待ってから30分の手順を始めると RTO を大きく超過する。
3. **修正済 (E-SVC-26-4)** 課題26.4 の必要サービス「OpenSSL/TLS」 ― プロセス内の `Map` と `node:crypto` で完結する。
4. **修正済 (E-P6-017)** Chaos Mesh の例でコメント「毎日10分だけ」と cron の平日指定 (`1-5`) が矛盾し、`scheduler` フィールドが v1 系の書式のままだった。
5. **機械化** 所要時間「6-8時間」対 実合計660分。

**中程度**
6. **修正済 (E-P6-016)** 「TCP のデフォルトは「**永久に待つ**」(SO_KEEPALIVE で数時間)」 ― 接続確立にはOSレベルの期限がある。期限が無いのは確立済み接続の読み取り待ち。
7. **修正済 (E-P6-015)** 「Amazonの「Prime Video が逆にマイクロサービスからモノリスに戻して 90%コスト削減」事例は**象徴的**」 ― 実際は1チームの1機能 (音声・映像品質監視) の話で、「Amazon がマイクロサービスをやめた」ではない。本書全体で最も引用ミスが起きやすい事例であり、範囲を限定した。
8. **見送り** `retryWithBackoff` が最終試行後にも待ってから投げる件 ― 演習カードのヒントが正しい順序を示しており、読者は気づける。KEN-63 へ。
9. **見送り** 冪等性ミドルウェアの GET → SET NX の競合窓と、演習カードの in-flight 合流方針との食い違い ― 本文と演習で推奨が割れているが、どちらも成立する設計であり、統一は設計判断を要する。KEN-63 へ。
10. **機械化** 「コード集の使い方」の `npm install`。

**軽微**
11. **修正済 (E-VAG 系)** 「現代の主流: モジュラモノリス」「数千万ユーザーまで戦える」「ほとんどのスタートアップは Pilot Light で十分」の3件。
12. **修正済** 「ヘルスチェック以外の集計、推薦など」の係り受けの曖昧さ。

---

## 第VII部 実践編 (第27〜30章)

### 第27章 設計とドメインモデリング
指摘件数: 11件 (修正済 7 / 機械化 2 / 見送り 2)

**重大**
1. **修正済 (E-P7-001)** `CreateOrderUseCase` のコンストラクタが `customerRepo` を受け取っていないのに `this.customerRepo.findById(...)` を呼んでいた ― Clean Architecture の依存注入を説明する中核コード例が動かなかった。
2. **修正済 (E-P7-002)** 通貨換算 `convert` が、換算元の最小単位整数をそのまま換算先通貨の最小単位として再解釈していた ― JPY(0桁)→USD(2桁) では `¥1000` が `$10.00` として扱われ100倍ずれる。「通貨ごとに小数桁が違う」ことを説く節そのものが小数桁の差を無視していた。
3. **修正済 (E-P7-003)** `allocate` の余り配分ループが `remainder > 0n` を条件にしており、`total` が負 (返金の按分) のとき一度も回らず、節が「絶対の制約」と宣言した「戻り値の合計が常に total と等しい」を破っていた。返金・取消の按分は同節が明示的に用途に挙げている。
4. **機械化** 「コード集の使い方」の `tsx clean-arch/main.ts` は存在せず、他3ファイルも export のみで実行時出力を持たない。

**中程度**
5. **修正済 (E-P7-004)** 「**次節の**課題27.5 では」 ― 次節は 27.18。課題27.5 があるのは 27.19。
6. **修正済** 四捨五入の表で `Math.round` を half-up として説明していた件 (実際は half-ceiling)。
7. **見送り** `Order.total()` が `new Money(0, 'JPY')` をハードコードしている件 ― 直後の 27.18 が「通貨をコードの分岐にしない」と説くのと逆行するが、集約の説明が主題の簡略例。
8. **見送り** `bigint` を採る理由と `format()` の `Number()` 経由の矛盾 ― 表示用途であることは文脈から読める。

**軽微**
9. **機械化** 所要時間 (本文は範囲内だが `minutes` メタと倍近く違う件は全体所見2)。
10. **修正済** 「曖昧語は 27.17 で数値へ」という同趣旨の文が節内で3回反復していた件 ― 1回へ集約。
11-12. **修正済** 索引・表記の細部 (2件)。

### 第28章 大規模リファクタリングとレガシー対応
指摘件数: 12件 (修正済 9 / 機械化 2 / 見送り 1)

**重大**
1. **機械化** `tsx adr-gen.solution.ts new "First decision"` (KEN-48 が名指しした箇所) ― `adr-gen.solution.ts` は関数を export するだけで `process.argv` を読む CLI 入口を持たず、この行を実行してもファイルは1件も生成されない。しかも同ブロックの4行すべてが export のみで無反応だった。
2. **修正済 (E-SVC-28-3)** 課題28.3 の必要サービス「PostgreSQL」 ― `node:fs/promises` だけを使う。
3. **修正済 (E-P7-009, E-P7-010)** 強調記号 `**` が閉じておらず Markdown が壊れていた2箇所。
4. **機械化** 所要時間「5-7時間」対 実合計480分。

**中程度**
5. **修正済 (E-P7-005)** 「Netscape が Mozilla を書き直す決定で市場を失った」 ― 因果が逆。Netscape が Navigator を全面的に書き直し、その成果物が Mozilla。
6. **修正済 (E-P7-006)** 「「設計が酷い、書き直そう」は**7割の確率で**失敗する」の出典なき定量的断定。
7. **修正済 (E-P7-007)** 「コードベース全体で **70%+ のカバレッジ**」 ― 出典なき閾値であり、第25章のカバレッジ目標への立場と整合しなかった。
8. **修正済 (E-P7-008)** 「日本の個人情報保護法。**2022年改正**で強化。」 ― 2020年改正法が2022年4月に全面施行。
9. **見送り** ADR の連番例が 001/002 から突然 003/010 に飛ぶ件 ― 番号の対応が追えないが、Superseded の説明としては成立する。

**軽微**
10. **修正済 (E-VAG 系)** 「現代の高速デプロイ環境では Trunk-Based Development が**主流**」に判断理由を添えた。
11. **修正済** 節タイトル「コードレビューの**ベストプラクティス**」 ― 他節が機能で命名されている中で浮いていた。
12-13. **修正済** 索引・表記の細部 (2件)。

### 第29章 LLMを組み込むWeb開発 (旧「AI時代のWeb開発」)
指摘件数: 15件 (修正済 11 / 機械化 2 / 見送り 2)

**重大**
1. **修正済 (E-TITLE-029-*)** 章タイトル「AI時代のWeb開発」 ― 「AI時代」は時点も定義も持たない曖昧語 (`config/style-guide.json` の `vagueExpressions` に登録済み)。章の内容は LLM の組み込みであり、「LLMを組み込むWeb開発」へ改題した。関連8ファイルを同時更新。
2. **修正済 (E-P7-011)** `server.setRequestHandler('tools/list', ...)` ― `@modelcontextprotocol/sdk` の `setRequestHandler` はメソッド名の文字列ではなく Zod スキーマを取る。そのままでは型エラーで動かない。
3. **修正済 (E-P7-012)** 「JSON Schema に従わないトークンを生成しないよう制限する。**確実に有効な JSON が出力される。**」 ― 拒否応答 (refusal) と打ち切りがあるため無条件の保証にはならない。同章の演習カードの警告と噛み合っていなかった。
4. **修正済 (E-P7-013)** 「Structured Outputs (**2024 年後半に OpenAI、Anthropic、Google が次々と対応**)」 ― 2024年後半に同名機能を出したのは OpenAI (2024年8月)。他社は機能名も時期も異なる。
5. **見送り** Vercel AI SDK の例が v4 と v5 の API をまたいで混在している件 (`await streamText`、`parameters`、`maxSteps`、`toDataStreamResponse`、`ai/react`) ― SDK の版を明記して一方へ統一する判断が要る。KEN-63 へ。
6. **機械化** 所要時間「6-8時間」対 実合計570分。
7. **機械化** 「コード集の使い方」の5行が export のみで無反応、`mini-mcp-server.solution.ts` だけ stdio サーバとして起動して固まる。

**中程度**
8. **見送り** RAG の実装例が index 側 (`JSON.stringify` で文字列保存) と query 側 (`::vector` キャスト) で型が食い違う件 ― pgvector の使い方全体の書き直しになる。KEN-63 へ。
9. **修正済** `openai.beta.chat.completions.parse` の beta 名前空間とモデル指定の固定。
10. **修正済 (E-P7-018)** MCP 節の「急速に普及した」「次々対応」「主要 SaaS が次々と公開」を、時点と確認できる事実へ。
11. **修正済** 「ChatGPT、Gemini: 互換性のある実装を提供」の意味不明な記述。
12. **修正済** 「MCP specification 2026-07-28」の版の裏取りと参照の整理。
13. **修正済** 「1億ベクトルを超えるなど専用が必要になったら」の閾値に条件を添えた。

**軽微**
14. **修正済** 「長期的なエンジニアとしての**勝ち筋**」のくだけた語。
15. **修正済** 「表面的な「ChatGPTに頼んでコピペ」とは違う、深い技術理解こそが、価値を生み続ける。」の根拠を伴わない価値主張。

### 第30章 総合演習 ― 本番品質のSaaSをゼロから構築
指摘件数: 14件 (修正済 11 / 機械化 0 / 見送り 3)

**重大**
1. **修正済 (E-P7-014)** 「Stripe Elements / Checkout に任せれば、**PCI DSS Level A (最低レベル)** で済む」 ― PCI DSS に「Level A」という区分は存在しない。あるのは自己問診票の **SAQ A** と取引量で決まる加盟店 Level 1〜4。両者の取り違えだった。
2. **修正済 (E-SVC-30-1)** 課題30.1 の必要サービス「PostgreSQL、Kubernetes」 ― 模範解答は `node:http` と `Map` で動く18行のスクリプトで、カード自身の警告 (「ストアがメモリ上のMap」) とも矛盾していた。
3. **修正済 (E-GAP-301, E-GAP-301b)** 「模範解答: `code/ch30/saas/`(本書最大の解答、複数ファイル構成)」と `src/domain/` 以下の実装単位 ― 実体は `starter/main.sh` と `solution/main.sh` の2ファイル。設計案であることを明示し、読者の実装範囲を切り分けた。
4. **修正済 (E-P7-017)** タスク一覧のカーソルページネーションが `cursor: { id }` と `orderBy: { createdAt }` で基準列が一致せず、`skip: 1` も無いためページ境界で取りこぼしと重複が起きる。
5. **修正済 (E-P7-015)** 「SRE 経験は**本でしか得られない**学びを与える」 ― 意味が逆 (文脈は実務経験を勧める段)。
6. **修正済 (E-P7-016)** 「本書の全 **158 課題**、**200,000 行以上**のコードを書いた」 ― 実測は課題147件、`code/` 配下のコード約14,974行。読者が最初に信頼を落とす種類の誤り。

**中程度**
7. **見送り** 30.8 と 30.13.1 で Stripe 連携が二重に扱われ、30.8 側が劣化版 (`req.rawBody`、try/catch 無し) になっている件 ― 30.8 を設計方針に絞り実装を 30.13.1 へ集約すべきだが、章構成の変更になる。KEN-63 へ。
8. **見送り** Outbox ポーラに未処理フラグの絞り込みと `FOR UPDATE SKIP LOCKED` 相当が無く二重投入しうる件 ― 第17章 17.11 が正しい形を示している。KEN-63 へ。
9. **修正済** 「課題17.6 は C と D を再現する」 ― 課題17.6 は D (外部API連携) のみで、C (メール配送) を扱う演習は第17章に存在しない。
10. **修正済** ダイジェスト送信の判定でコメント (現地時刻) と実装 (UTC) が食い違っていた件。
11. **修正済** `fetch('/upload/init', ...)` に `Content-Type: application/json` が無く `express.json()` がパースしない件。
12. **見送り** `sendEmailSafe` の引数が `...` で構文として成立しない件 ― 擬似コードであることが文脈から読める。

**軽微**
13. **修正済** 「レポート のみ」の不要な半角スペース、「**第**30.17節」の重複。
14. **修正済** 30.16 冒頭とまとめの説明がほぼ同一語句で繰り返されていた件。

---

## まとめ・終章
指摘件数: 3件 (修正済 3 / 機械化 0 / 見送り 0)

- **修正済** 第I部総括の「TCPとTLSが、メッセージを運び」 ― 2.7 で HTTP/3 が QUIC を使うと説明した内容が総括で TCP 前提に戻っていた。
- **修正済** 第II部総括の「単に「モダンな道具が増えた」というものではない」の曖昧語。
- **修正済** 終章の課題数・行数 (第30章 6 と同一)。

第III部・第IV部・第V部・第VI部・第VII部の各総括は、いずれも章の実際の内容と一致しており、事実誤り・参照ずれは見つからなかった (指摘0件)。

---

## 文体の確認結果

本文8ファイルについて、コードブロック・引用符内 (UI文言・発話例) を除いた地の文の敬体文末を機械的に数えた。

```
本文の敬体文末 (引用符内・コード・引用ブロックを除く): 0
```

`validate:style` の S-JA-007 (本文の文体は常体に統一する、enforcement: error) も **errors: 0** で通る。通読でも、地の文に「です・ます」が混入している箇所は見つからなかった。検出された「です・ます」はすべて、かぎ括弧内の UI 文言 (「メールアドレスが存在しません」)、発話例 (顧客の情シス担当「うちはOktaなんですが」)、認証器の応答例 ("私は本物の YubiKey です") であり、STYLE_GUIDE の除外に合致する。

読者への呼びかけは、第1章の導入 (「準備はいいだろうか」「見ていこう」) と「〜してほしい」が計9箇所残る。いずれも常体であり、章の入口に置かれた語りかけとして意図的なものと判断し、統一の対象から外した。
