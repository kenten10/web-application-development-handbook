# 概念を有機的につなぐ編集ガイド

本書の技術的な正確性と網羅性を維持しながら、読者が知識を断片ではなく因果関係のあるモデルとして構築できる文章へ編集するための基準を定める。

## 1. 基本原則

新しい概念は、定義から始めない。まず、直前までの知識だけでは解けない具体的な問題または制約を示す。そのうえで、新概念を「その不足を補う仕組み」として導入する。

標準的な流れは次のとおり。

1. **到達点**: 直前までに何ができるようになったか
2. **不足**: その知識だけでは何が解けないか
3. **要求**: 解決に必要な性質は何か
4. **概念**: その性質を実現する仕組みは何か
5. **接続**: 既存概念とどう組み合わさるか
6. **観測**: コード・ログ・計測で何を確認するか
7. **残る問い**: 次の節または章で何を解くか

## 2. 章の構造

### 2.1 章冒頭

章冒頭は、単独のキャッチコピーや定義ではなく、次の3点を文章で示す。

- 前章までに完成した理解
- その理解に残る限界
- 本章が限界をどう埋めるか

悪い例:

> 状態管理は重要である。本章ではReduxとQuery Cacheを扱う。

良い例:

> コンポーネントは局所的な状態を持てるが、同じデータを離れた画面で共有し、サーバから再取得し、失敗時に元へ戻す処理まで各コンポーネントへ埋め込むと、更新規則が分散する。この分散を解くため、本章では状態を種類と所有者で分け、クライアント状態とサーバ状態に異なる管理方法を与える。

### 2.2 主要節の冒頭

主要節の最初の段落は、前節の内容を一文で再掲するのではなく、前節で残った具体的な問題を受け取る。

- 「Aを学んだ。次にBを学ぶ」だけにしない
- Aだけではなぜ不足するかを書く
- Bが追加する保証・責務・抽象化を示す

### 2.3 章末

章末は用語の箇条書きだけで終えない。読者のモデルがどのように更新されたかを文章で統合する。

- 章の概念を一つの処理や判断の流れへ戻す
- 障害時にどの順序で観測するかを示す
- 次章を必要とする未解決の問いを残す

## 3. 概念の導入方法

### 3.1 問題から導入する

- DNS: 人間向けの安定した名前を、通信時点のIPへ変換する必要から導入
- TLS: 宛先が分かっても、相手の正当性と通信内容の保護が不足することから導入
- フレームワーク: 状態とDOMの同期規則が分散する問題から導入
- メッセージキュー: 同期リクエストへ副作用を詰め込むと遅延と障害が結合する問題から導入
- 可観測性: 予期した故障だけを監視項目にすると、未知の故障を説明できない問題から導入

### 3.2 同じ概念を別の層へ再接続する

本書では、後の章で以前の概念を別の文脈へ戻す。

- HTTPの冪等性 → API設計 → リトライ → メッセージ処理 → 決済・SaaS総合演習
- ステートレス性 → セッション → 水平分割 → コンテナ → ロードバランサ
- イベントループ → フロントエンド非同期処理 → Node.jsランタイム → バックプレッシャー
- キャッシュ → HTTPヘッダ → Query Cache → Redis → CDN → 整合性
- 信頼境界 → TLS → 認証・認可 → SSRF → Secret管理 → サプライチェーン

再登場時は「既に学んだ概念」で済ませず、今回どの性質を再利用し、どの前提が変わるかを書く。

## 4. 文体

- 短い断定文を連続させず、因果関係を一つの段落にまとめる
- 「完全」「革命」「最強」「時代遅れ」のような煽りを、条件と目的を含む説明へ置き換える
- 比喩は理解を助ける場合だけ使用し、比喩だけで技術的理由を済ませない
- 読者への命令や叱責ではなく、観測可能な問題と判断基準を示す
- 用語を列挙するときは、それらの役割分担または選択軸を先に示す
- 遠い章を参照するときは、現在必要な理解範囲と後で拡張する内容を区別する

## 5. 技術内容を壊さないための制約

- 技術的主張、数値、引用、RFC参照の意味を編集上の都合で変更しない
- コードブロック、アンカー、学習メタデータ、索引メタデータを保持する
- 生成された `handbook:chapter-guide` ブロックは直接編集しない
- 新しい技術的主張を追加した場合は、技術校閲と出典確認の対象にする
- 接続文は、既存内容から論理的に導ける範囲に限定する

## 6. `minimumBridgeCount` の意味と据え置きの判断

`config/narrative-flow.json` の `minimumBridgeCount` は、その章が持つべき節間接続の**下限**である。`scripts/validate-narrative-flow.mjs` は実数がこの値を下回ったときだけ失敗させる。実数が下限を上回っているのは不整合ではなく、意図的な余裕である。

2026年8月時点で、第12章は実数16に対し下限13、第27章は実数19に対し下限13である。KEN-61 で実数へ更新するかを検討し、**据え置く**と判断した。理由は3つある。

1. **下限を実数へ引き上げると、原稿が壊れやすくなる。** 以後、編集で接続文を1つ減らしただけで検証が失敗する。減らしてよい場面まで検証が止めることになり、下限としての意味を失う。
2. **選定基準が相対値のため、連鎖する。** `beta-review-scope.json` の基準C4は「節間接続数が全章の第75パーセンタイル以上」である。2章の値を上げるとしきい値が14から15へ動き、第12章と第27章が必須検証章へ入り、第20章と第29章がC4から外れる。必須検証章は15章から17章になり、基準E2により課題12.2と課題27.3 (各150分) が必須検証演習へ加わって37件から39件になる。
3. **追加される演習は未検証になる。** 上記2件はKEN-61のベータレビューで実施していない。この時点で範囲を広げると、release blocker RB-03 (必須検証演習が未実施のまま残っている) が新たに成立してしまう。

下限は13のまま据え置き、実数がそれを上回る状態を正しい状態とする。将来この値を実数へ寄せる場合は、上記の連鎖を踏まえ、`beta-review-scope.json` の再生成と、新たに必須となる演習の実施までを1つの作業として扱うこと。

## 7. レビュー基準

各章について次を確認する。

- 章冒頭だけで、前章との関係と本章の必要性を説明できる
- 各主要節の見出しを隠しても、冒頭段落から扱う概念を予測できる
- 「なぜ今この概念が出るのか」に本文が答えている
- 章末が用語の再掲ではなく、処理・設計・診断の流れを統合している
- 次章への移行が話題変更ではなく、未解決問題の引き継ぎになっている
- 単独参照時にも理解でき、通読時には重複説明が過剰でない
